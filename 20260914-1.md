# 2026-09-14 개발일지 1

## 오늘의 목표

기존 프레임워크에서 발견한 불편과 Tonic의 계약 중심 코드 생성 방식을 다시 검토하고, NecrassRs의 SDL 작성부터 빌드·실행까지의 개발 워크플로우를 결정한다.

## 변경 사항

이번 기록은 대화에서 합의한 설계 결정이다. CLI, SDL 파서, 코드 생성기, 빌드 연동을 이번 작업에서 구현한 것은 아니다.

`necrassrs` 작업 트리에는 변경이 없었다. `necrassrs-lab`에는 기존 미커밋 변경이 있으며, 이번 기록 및 커밋의 대상에는 포함하지 않는다. 기존 설계 근거는 [9월 5일 설계 조사](research/20260905-necrassrs-design.md)를 참고한다.

## 논의 및 관찰

### 첫 해결 대상

기존 실험에서 확인한 주요 부담은 스키마·타입 선언, DB row 변환, 관계 조회 연결, 페이지네이션, Mutation 입력·출력, 타입 및 root 등록의 반복이다. 첫 해결 대상은 공개 GraphQL 계약과 Rust 구현 사이의 타입·필드 선언 및 연결 코드 중복으로 유지한다.

단순 getter는 async-graphql의 `SimpleObject`와 `ComplexObject`로 줄일 수 있고, DataLoader 등 기존 기능도 존재한다. 따라서 기존 기능의 부재를 전제로 생산성 우위를 주장하지 않는다. 관계 로딩 및 SQL 자동화는 선언 반복 감소 이후 검증할 별도 과제다.

### Tonic에서 가져올 접근법

Tonic의 기본 Protobuf 개발 흐름은 `.proto` 계약에서 메시지 타입, 서비스 trait, 서버 연결 코드 등을 생성하고, 사용자가 별도 코드에서 서비스 trait을 구현하는 방식이다. NecrassRs에서는 이에 대응하는 공개 계약으로 `*.graphql` SDL 파일을 사용한다.

생성 파일에 `todo!()`를 넣고 사용자가 직접 수정하는 방식은 채택하지 않는다. 생성 코드와 사용자 구현을 분리하여 SDL 변경 후 재생성해도 업무 로직이 보존되어야 한다. 필수 resolver 구현 누락은 컴파일 오류로 드러내는 것을 목표로 한다. `todo!()`는 컴파일을 통과하고 실행 시 패닉을 발생시키므로 구현 완료를 확인하는 수단이 아니다.

## 결정 사항

### 개발 워크플로우

1. `necrass init`으로 프로젝트를 준비한다. 최초 SDL, Cargo 설정, 생성기를 호출하는 `build.rs`, 서버 진입점을 만든다.
2. 사용자가 `*.graphql`에 공개 API의 타입, 필드, 인자, nullability를 정의한다.
3. `cargo build`가 `build.rs`를 통해 SDL에서 Rust 코드를 자동 생성하고 컴파일한다. 타입, resolver trait, 필드 연결 및 등록 코드는 Cargo의 `OUT_DIR`에 생성한다. 필요한 사용자 구현이 없으면 컴파일 오류로 안내한다.
4. 사용자는 `src/`의 별도 파일에서 생성된 trait을 구현한다. 조회, 계산, Mutation 업무 로직은 사용자 코드에 작성하고 생성 파일은 직접 수정하지 않는다.
5. 이후에는 SDL과 사용자 코드를 수정하고 `cargo build`한다. SDL 변경 시 생성 코드가 갱신되고, 구현과 계약의 불일치는 컴파일러가 알려준다. 서버는 `cargo run`으로 실행한다.

초기 설정 이후 코드 생성을 위한 별도 선행 명령은 요구하지 않는다. `necrass compile`은 우선 만들지 않는다. 빌드 시 자동 생성과 사용자 구현 골격 생성을 하나의 역할로 혼동하지 않는다.

### 책임 분리

| 구성 요소 | 책임 |
| --- | --- |
| `*.graphql` | 공개 API 계약의 기준 |
| SDL 파서 및 검증 단계 | SDL을 파싱하고 타입 참조 및 스키마의 의미적 유효성을 확인 |
| 코드 생성기 | Rust 타입, 필요한 resolver trait, 기본 필드 연결 및 스키마 등록 코드 생성 |
| `build.rs` | Cargo 빌드에서 생성기를 실행하고 SDL 변경을 재생성에 반영 |
| `OUT_DIR`의 생성 코드 | 재생성 가능한 계약과 연결 코드 보관 |
| `src/`의 사용자 구현 | 데이터 조회, 계산, 도메인 검증 및 상태 변경 구현 |
| GraphQL 실행기 | 요청 검증, 변수·입력 처리, 필드 실행, null 전파, 오류 및 부분 응답 처리 |

기존 GraphQL SDL 파서를 재사용하는 방향으로 검토한다. 구체적인 라이브러리는 아직 선택하지 않았다. 파싱, 스키마 검증, Rust 코드 생성은 구분되는 단계이며 파싱 성공만으로 유효한 스키마가 보장되지는 않는다.

단순 필드를 모두 수동 trait 메서드로 구현하게 하여 getter 반복을 재현하지 않는다. 기본 필드는 자동 연결하고, 계산·관계·업무 로직 등 사용자 구현이 필요한 부분에 resolver 구현 의무를 집중한다. 구체적인 Rust 바인딩 및 생성 API는 후속 설계가 필요하다.

SDL만으로 DB 조회 방법이나 권한 정책을 추측하지 않는다. 코드 생성만으로 GraphQL 실행 의미론이나 N+1 문제가 해결되는 것도 아니다. 실행기 선택과 관계 로딩 자동화는 이번 워크플로우 합의와 별도다.

## 검증

- 변경 수집 스크립트로 저장소 상태와 한국 시간 기준 일지 날짜 `20260914`를 확인했다.
- 이번 작업은 설계 일지 작성만 수행하므로 코드 테스트와 서버 실행은 수행하지 않았다.
- 위 워크플로우는 합의한 목표이며 실제 구현·동작 검증 결과가 아니다.

## 다음 작업

1. 기존 SDL 파서 후보의 AST와 검증 지원 범위를 확인한다.
2. 작은 SDL을 기준으로 생성 타입, resolver trait, 사용자 구현 및 서버 연결의 최소 형태를 설계한다.
3. `build.rs`와 `OUT_DIR`을 사용하는 최소 프로토타입으로 `cargo build`만의 생성·컴파일 흐름을 검증한다.
4. SDL 변경 시 재생성, 필수 구현 누락의 컴파일 오류, 사용자 코드 보존을 확인한다.
