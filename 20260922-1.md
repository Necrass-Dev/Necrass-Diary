# 2026-09-22 개발일지 1

## 오늘의 목표

이슈 #2의 실행 코어 구현을 마무리하면서 Apollo Compiler의 요청 검증과 입력 coercion 경계에서 발견한 문제를 재현하고, 프로세스가 종료될 수 있는 경로를 NecrassRs 내부에서 방어한다.

## 변경 사항

### 중첩 변수 기본값을 NecrassRs에서 다시 정규화

Apollo의 `request::coerce_variable_values`를 호출한 뒤에도 변수 선언의 input object 기본값 내부에 있는 필드 기본값과 단일 값의 리스트 변환이 기대한 최종 JSON 형태까지 적용되지 않는 사례를 확인했다.

예를 들어 변수 기본값이 빈 input object이고 그 타입의 필드가 문자열 기본값 또는 리스트 타입의 단일 값 기본값을 가지면, resolver에 전달할 값에는 해당 필드 기본값과 리스트 변환이 반영되어야 한다. 처음에는 Apollo의 변수 coercion을 한 번 더 호출하는 방식도 검토했지만, 변환 책임과 종료 조건이 불분명했다.

`request.rs`에 다음 단계를 추가했다.

1. Apollo의 공개 API로 요청 변수의 기본 검증과 coercion을 수행한다.
2. 반환된 `JsonMap`을 변수 선언 타입에 맞춰 `coerce_json_input_value`에서 재귀적으로 정규화한다.
3. 리스트는 단일 값을 한 항목의 배열로 변환한다.
4. input object는 생략된 필드의 스키마 기본값을 적용하고 필드 타입에 따라 다시 coercion한다.
5. Apollo가 앞 단계에서 검증한 결과를 `Valid::assume_valid`로 다시 감싼다.

GraphQL AST 기본값을 JSON으로 바꾸는 로직은 `input.rs::literal_to_json`으로 옮겼다. 실행 단계의 리터럴 인자와 요청 준비 단계의 변수 기본값이 같은 변환 코드를 사용하도록 하여 숫자 범위 오류와 unresolved variable 오류 처리를 한곳에 유지했다.

관련 커밋은 `5b2a1db`, `e0d9906`이다.

### introspection 메타 필드의 검증 결과와 스키마 조회 구조가 달랐던 문제

`__schema`와 `__type`은 요청 문서 검증을 통과하지만 일반 object의 `fields` 컬렉션에는 존재하지 않았다. 실행기가 검증된 필드는 항상 `object_type.fields`에서 찾을 수 있다고 가정하고 `expect`를 사용하자, 다음 요청들이 panic으로 이어질 수 있었다.

```graphql
{ __schema { queryType { name } } }
```

```graphql
{ __type(name: "Query") { name } }
```

필드 정의 조회를 `schema.type_field(parent_type, field_name)`로 변경해 Apollo가 제공하는 메타 필드 정의까지 포함하도록 했다. 현재 MVP는 전체 introspection 실행을 지원하지 않으므로 `__schema`와 `__type`은 dispatcher를 호출하거나 panic하지 않고 명시적인 GraphQL 실행 오류를 반환한다. `__typename`은 별도 분기에서 현재 root 타입 이름을 반환한다.

이 문제는 Apollo의 검증 결과가 잘못되었다기보다, 실행기에서 일반 object 필드 저장소와 메타 필드 조회 API를 같은 것으로 가정한 통합 오류였다. `Valid<ExecutableDocument>`만 보고 내부 조회 위치까지 추측해서는 안 된다는 점을 확인했다.

관련 커밋은 `e469c2d`, `d04a8bf`이다.

### 순환 input 기본값을 유효한 스키마로 받아들이는 문제

Apollo Compiler 1.32.0과 1.33.0은 다음 스키마를 `Schema::parse_and_validate`에서 유효한 것으로 받아들였다.

```graphql
input Recursive {
  next: Recursive = {}
}

type Query {
  hello(input: Recursive): String
}
```

GraphQL September 2025 명세의 input object 타입 검증에는 `InputObjectDefaultValueHasCycle` 규칙이 있다. 위 스키마는 `next`가 생략될 때 기본값 `{}`를 다시 적용하는 과정이 끝나지 않으므로 거부되어야 한다. 그러나 Apollo가 반환한 `Valid<Schema>`만 신뢰한 상태에서 NecrassRs가 기본값을 재귀적으로 확장하자 스택 오버플로가 발생했고, 테스트 프로세스가 `SIGABRT`로 종료됐다.

순환은 두 경로에서 각각 발생했다.

- 변수 경로: `query($input: Recursive = {}) { hello(input: $input) }`는 `request.rs`의 JSON 입력 정규화를 반복했다.
- 리터럴 경로: `{ hello(input: {}) }`는 `execution.rs::coerce_input_value`에서 생략된 `next`의 기본값을 반복했다.

두 coercion 경로에 현재 확장 중인 기본값의 `(input type, field)` 좌표를 저장하는 집합을 전달했다. 생략된 필드 때문에 스키마 기본값을 적용할 때만 좌표를 추가하고, 재귀 처리가 끝나면 제거한다. 이미 활성화된 좌표에 다시 진입하면 `cyclic default value` 오류를 반환한다. 명시적으로 주어진 재귀 값과 서로 다른 리스트 항목에서 유한한 기본값을 반복 사용하는 경우는 차단하지 않는다.

스택 오버플로는 같은 테스트 프로세스에서 `catch_unwind`로 안전하게 검증할 수 없으므로 회귀 사례를 자식 프로세스에서 실행했다. 변수 경로와 실행 경로 모두 자식 프로세스가 정상 종료하고, 실행 경로는 resolver를 호출하지 않으며 alias가 반영된 오류 path와 nullable field의 `null`을 반환하는지 확인한다. 리터럴 입력, 생략된 인자, 값이 없는 인자 변수, 값이 없는 input field 변수, 리스트를 통한 상호 순환을 포함했다.

관련 커밋은 `8b615fb`, `cb2dc4b`, `198927f`, `a971a27`, `3b5ad63`이다. 중복된 단일 리터럴 회귀 테스트는 더 넓은 실행 경로 테스트가 같은 사례를 포함하므로 `bbf4252`에서 제거했다.

## 구현 메모

Apollo Compiler는 1.33.0으로 갱신해 고정했다. 현재 0.x 단계에서는 스키마 모델, 요청 파싱과 검증, operation 선택, 공개 변수 coercion API를 재사용하는 편이 구현 속도에 유리하다. 다만 `Valid<T>`는 NecrassRs가 의존하는 모든 September 2025 규칙과 실행기 전제까지 보장하는 표식으로 취급하지 않는다.

1.33.0 changelog의 기능 및 수정 사항은 `@defer` 사용 검증과 query·mutation·subscription root 타입 중복 검증이다. 오늘 확인한 중첩 input 기본값 정규화, 순환 기본값 검증, 메타 필드 조회 문제는 수정 목록에 없었다. 배포된 1.32.0과 1.33.0의 `resolvers/input_coercion.rs`도 동일했으므로 기존 방어 코드를 유지했다.

Apollo를 감싸는 별도 schema crate나 범용 호환 계층은 만들지 않았다. 지금 확인된 결함을 막기 위해 요청마다 전체 스키마를 다시 검사하는 것도 피했다. 실제로 재귀 확장이 일어나는 공통 coercion 흐름에서 활성 기본값만 추적하는 방식이 현재 범위에서 가장 작고 직접적인 방어였다.

Apollo의 field-argument coercion 구현은 소비자가 사용할 수 있는 공개 API가 아니므로 리터럴 인자와 input object 기본값 처리는 NecrassRs 실행기가 담당한다. 이 때문에 Apollo의 AST와 스키마 모델을 사용하더라도 argument coercion, 오류 path, result completion과 null propagation은 자체 회귀 테스트가 필요하다.

## 실험 및 관찰

- `Schema::parse_and_validate`가 성공해도 메타 필드가 일반 object field 컬렉션에 있다는 뜻은 아니었다.
- `coerce_variable_values`가 성공해도 NecrassRs가 resolver에 전달하려는 중첩 JSON 기본값이 모두 정규화되었다고 가정할 수 없었다.
- Apollo가 `Valid<Schema>`로 반환한 스키마에서도 기본값 순환이 남을 수 있었다.
- 순환 감지는 타입 이름만으로 하면 유효한 재귀 구조를 과도하게 거부할 수 있다. 실제로 기본값을 적용 중인 필드 좌표를 추적해야 한다.
- 순환 추적 상태는 한 coercion 호출이 끝난 뒤 제거해야 한다. 그렇지 않으면 서로 다른 리스트 항목이나 인자에서 같은 유한한 기본값을 사용하는 정상 사례가 오탐된다.
- 프로세스 abort 가능성이 있는 회귀는 자식 프로세스 격리가 필요했다.

`necrassrs-lab`에는 오늘 변경이 없었다.

## 결정 사항

1. Apollo Compiler 1.33.0은 당분간 빠른 MVP 구현을 위한 의존성으로 유지한다.
2. Apollo의 공개 API와 검증 표식은 해당 API가 실제로 보장하는 범위에서만 신뢰한다.
3. Apollo 통합 경계에서 확인한 결함은 작은 회귀 테스트와 실행 시 방어로 고정한다.
4. 스키마 전체를 감싸는 추상화나 Apollo 교체 작업은 이번 이슈에 포함하지 않는다.
5. 장기적으로 Apollo를 교체할 때는 오늘 만든 입력 기본값, 메타 필드, 순환 종료 회귀 테스트를 새 구현의 호환 기준으로 사용한다.

## 검증

- `cargo fmt --all -- --check`
- `cargo test --workspace --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- 단위 테스트 48개와 통합 테스트 4개가 통과했다.
- Apollo Compiler 1.33.0으로 갱신한 뒤에도 같은 검증 명령과 순환 기본값 회귀 테스트가 통과했다.
- 이슈 #2 구현을 `2/execution-core` 브랜치에 푸시하고 PR #7을 열었다.

## 다음 작업

1. PR #7의 리뷰 결과에 따라 Apollo 통합 경계의 누락된 회귀를 보강한다.
2. 현재 명시적으로 오류를 반환하는 introspection과 지원하지 않는 result type의 구현 순서를 별도 이슈에서 정한다.
3. Apollo 버전 변경 시 중첩 기본값 정규화와 순환 기본값 검증이 upstream에서 해결되었는지 재확인한다.
4. Apollo 교체를 검토할 때 파싱, 검증, 타입 조회와 실행 입력 모델 중 실제로 필요한 최소 경계를 먼저 정의한다.
