# 2026-09-16 개발일지 2

## 오늘의 목표

SDL 코드 생성을 한 번 실행하는 명령과 변경을 계속 감시하는 명령의 관계를 정리한다.

## 변경 사항

[같은 날의 첫 번째 일지](20260916-1.md)에서 정한 `necrass watch`를 `necrass compile --watch`로 변경하고, 일회성 생성 명령인 `necrass compile`을 제공하기로 했다. 이번 작업은 설계 기록이며 CLI나 생성기를 구현한 것은 아니다.

## 논의 및 관찰

변경 감지에 따른 코드 재생성뿐 아니라, SDL 작성 직후 Rust 타입과 resolver trait을 한 번 생성하는 진입점도 필요하다. 일회성 실행과 반복 실행은 같은 작업이므로 별도 `watch` 명령보다 `compile` 명령의 옵션으로 묶는 것이 일관적이다.

Cargo만으로 생성과 빌드가 가능해야 한다는 요구가 명시적인 생성 명령을 배제하는 것은 아니다. 따라서 기존의 “`necrass compile`은 우선 만들지 않는다”는 결정을 수정한다.

## 결정 사항

### 개발 워크플로우

1. `necrass init`으로 최초 SDL, Cargo 설정, `build.rs`, 서버 진입점 등 프로젝트 구조를 준비한다.
2. 사용자가 `*.graphql`에 API를 정의한다.
3. `necrass compile`로 SDL을 검증하고 Rust 타입, resolver trait, 필드 연결 및 등록 코드를 한 번 생성한다.
4. 사용자는 `src/`의 별도 파일에서 생성된 trait을 구현한다.
5. 반복 작업 중에는 `necrass compile --watch`로 SDL 변경마다 코드를 재생성한다.
6. `cargo build` 또는 `cargo run`으로 Rust 프로젝트를 빌드하거나 서버를 실행한다.

별도 `necrass watch` 명령은 두지 않는다. 여기서 코드를 생성한다는 것은 재생성 가능한 계약과 연결 코드를 만드는 것이며, 사용자 업무 구현을 자동 수정하는 것이 아니다.

### 코드 생성과 Rust 구현 검사를 구분한다

`necrass compile`의 책임은 SDL 검증과 SDL에서 Rust로의 코드 생성이다. 필수 resolver 구현을 아직 작성하지 않았어도 유효한 SDL의 코드 생성 자체는 성공할 수 있어야 한다.

Rust 구현 누락이나 생성된 trait과의 불일치는 `cargo check` 또는 빌드 과정에서 검사한다. `compile --watch` 역시 변경 감지와 재생성을 담당하며, 생성 성공과 Rust 구현 검사 성공을 같은 결과로 취급하지 않는다. 이는 첫 번째 일지의 watch 흐름에 포함했던 `cargo check`와 코드 생성의 책임을 명확하게 나누는 수정이다.

### Cargo 단독 빌드 경로를 유지한다

CLI와 `build.rs`는 같은 생성기를 사용한다. `necrass compile`을 실행하지 않아도 `cargo build`가 최신 SDL을 반영하여 생성과 컴파일을 수행해야 한다. 명시적인 생성 명령은 선택 가능한 개발 경로이며 필수 선행 단계가 아니다.

기존 Cargo 경로의 생성 위치는 `OUT_DIR`로 설계했지만, 독립 실행하는 CLI와의 출력 위치 및 공유 방식은 아직 확정하지 않았다. 두 진입점의 생성물 경로와 프로젝트 연결 방식을 후속 설계에서 구체화한다.

SDL First, 생성 코드와 사용자 구현의 분리, 사용자 코드 보존 원칙은 유지한다. Rust 변경을 SDL에 역으로 자동 반영하거나, 삭제된 쿼리의 사용자 구현을 자동 삭제하지 않는다.

## 검증

- 변경 수집 스크립트로 기존 저장소 상태와 다음 일지 번호를 확인했다.
- `necrassrs`에는 작업 트리 변경이 없으며, `necrassrs-lab`의 기존 변경은 이번 작업에서 수정하지 않았다.
- 이번 작업은 문서 작성만 수행했으며 CLI, 코드 생성, Rust 빌드의 동작 테스트는 수행하지 않았다.

## 다음 작업

1. CLI와 `build.rs`가 사용할 공통 생성기 API 및 출력 위치를 결정한다.
2. 사용자 resolver 구현 전에도 일회성 코드 생성이 성공하는지 확인한다.
3. `compile --watch`와 Cargo 단독 빌드가 동일한 SDL 계약을 반영하고 사용자 코드를 보존하는지 검증한다.
