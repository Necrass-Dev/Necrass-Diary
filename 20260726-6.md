# 2026-07-26 개발일지 6

## 오늘의 목표

1차 async-graphql MVP의 Mutation을 완성하고, Pothos 구현과 비교해 쓰기 작업에서 발생하는 반복 비용을 관찰한다.

## 변경 사항

- `services/api-async-graphql-sqlx/src/graphql/create_issue.rs`
  - `createIssue` 입력과 페이로드를 정의했다.
  - 인증, 전역 ID 검증, 프로젝트 및 담당자 확인, 이슈 번호 생성, INSERT를 구현했다.
- `services/api-async-graphql-sqlx/src/graphql/update_issue.rs`
  - `MaybeUndefined`로 필드 생략, 명시적 `null`, 값 전달을 구분했다.
  - 기존 이슈와 담당자를 검증하고 변경된 필드를 UPDATE하도록 구현했다.
- `services/api-async-graphql-sqlx/src/graphql/add_comment.rs`
  - 댓글을 추가하고 이슈의 갱신 시각을 변경하는 트랜잭션을 구현했다.
- `services/api-async-graphql-sqlx/src/graphql/change_issue_status.rs`
  - 상태 enum을 DB 문자열로 변환하고 `UPDATE ... RETURNING`으로 변경 결과를 반환했다.
- `services/api-async-graphql-sqlx/src/graphql/mod.rs`
  - 네 Mutation object를 `MergedObject`로 Mutation root에 등록했다.
- `UserError::new`, 전역 ID codec, row-to-object 변환을 기존 구현에서 재사용했다.
- `cargo fmt --check`, `cargo check`, `git diff --check`로 현재 구현을 검증했다.

## 구현 메모

Mutation 하나를 추가할 때 다음 요소를 반복해서 작성해야 했다.

1. `InputObject`를 정의한다.
2. 성공 결과와 `UserError`를 포함하는 `Payload`를 정의한다.
3. 요청 context에서 인증 정보를 가져온다.
4. 전역 ID를 decode하고 기대한 Node 종류인지 확인한다.
5. 대상 및 relation entity의 존재 여부를 조회한다.
6. 입력값을 검증하고 DB 표현으로 변환한다.
7. INSERT 또는 UPDATE의 column, bind 순서, `RETURNING` projection을 작성한다.
8. SQLx row를 GraphQL object로 변환한다.
9. Mutation object를 root `MergedObject`에 등록한다.

`updateIssue`는 부분 갱신 때문에 특히 복잡했다. GraphQL 입력에서 필드 생략과 명시적 `null`은 의미가 다르므로 `Option<T>`만으로는 부족하고 `MaybeUndefined<T>`가 필요했다. 각 상태를 분기한 뒤 SQL bind 값까지 직접 조립해야 했다.

`addComment`는 댓글 INSERT와 이슈의 `updated_at` 변경을 하나의 트랜잭션으로 묶었다. 단순 relation mutation이라도 두 SQL의 오류 변환과 결과 mapping을 직접 처리해야 했다.

## 실험 및 관찰

Query의 반복도 컸지만 Mutation은 입력, 오류, 검증, 상태 변경이라는 추가 축이 있어 작업량이 더 많았다. Pothos와 Drizzle에서는 schema builder와 typed query API가 입력 및 반환 타입, relation, 부분 UPDATE를 연결해 주지만, async-graphql과 SQLx 조합에서는 GraphQL schema와 SQL mapping이 별개다.

특히 동일한 정보가 여러 위치에 반복된다. GraphQL 필드명, Rust 입력 필드, DB column, bind 순서, row projection, payload field를 각각 맞춰야 한다. 컴파일러가 일부 타입 불일치를 잡아 주지만, projection 누락이나 잘못된 bind 의미까지 하나의 선언에서 검증해 주지는 않는다.

반복을 줄이기 위해 범용 repository나 trait hierarchy를 추가하는 것은 이번 비교의 목적에 맞지 않는다. 인증, validation, transaction 경계는 Mutation마다 다르기 때문에 런타임 추상화로 감추면 코드량은 줄지 않고 제어 흐름만 멀어질 가능성이 높다.

이번 단계에서는 테스트를 추가하지 않았다. 실험의 목적은 프로덕션 수준의 테스트 구축이 아니라 Pothos와 async-graphql의 개발 경험 및 반복 비용 비교이므로 컴파일과 형식 검사까지만 수행했다.

## 결정 사항

- 현재 실험 코드에는 범용 Mutation repository를 만들지 않는다.
- 입력 및 페이로드 선언, 전역 ID 종류 검증, SQL projection과 row mapping, root 등록처럼 규칙적인 부분은 NecrassRs의 schema IR 또는 codegen 후보로 본다.
- 인증, 도메인 validation, 트랜잭션 경계는 생성 코드가 호출할 명시적인 hook으로 남긴다.
- `MaybeUndefined`의 생략, `null`, 값 의미는 생성 단계에서 보존해야 한다.

## 다음 작업

- async-graphql MVP와 Pothos 구현의 SDL 및 API 동작을 비교한다.
- Query와 Mutation에서 반복된 선언을 목록화하고 NecrassRs가 생성할 최소 범위를 정한다.
- 추가 기능 구현보다 schema IR과 codegen 경계를 먼저 설계한다.
