# 2026-09-15 개발일지 1

## 오늘의 목표

GraphQL October 2021 스펙의 기본 스칼라 coercion 규칙을 검토하고, 특히 `Boolean` result coercion의 구현 재량과 오류 처리 경계를 정리한다.

## 변경 사항

이번 기록은 GraphQL 스펙 해석과 NecrassRs 구현 기준 검토를 위한 문서 작업이다. `necrassrs` 코드 변경과 테스트는 수행하지 않았다. `necrassrs-lab`의 기존 미커밋 변경은 이번 기록의 대상에 포함하지 않는다.

참고한 규격은 [GraphQL October 2021 §3.5 Scalars](https://spec.graphql.org/October2021/#sec-Scalars)와 [§3.5.4 Boolean](https://spec.graphql.org/October2021/#sec-Boolean)이다.

## 구현 메모

### Result coercion과 input coercion

Result coercion은 리졸버가 반환한 내부 값을 GraphQL 응답 스칼라로 변환하는 단계다. 스펙은 내부 타입과 변환 과정이 클라이언트에 직접 노출되지 않으므로 정확한 변환 규칙을 구현체에 맡긴다. 구현체는 최종적으로 선언된 스칼라 타입에 맞는 값을 생성하거나, 변환할 수 없으면 field error를 발생시켜야 한다.

Input coercion은 클라이언트가 제공한 값을 서버 내부 값으로 변환하는 단계다. 입력값의 승인과 거부는 클라이언트가 관찰할 수 있으므로 규칙이 명확하다. `Boolean` 입력에는 `true`와 `false`만 허용되며 숫자 `0`, `1`이나 문자열 `"true"`, `"false"`는 request error다.

### Boolean result coercion의 모호성

스펙은 비-Boolean 내부 값을 `Boolean`으로 합리적으로 변환할 수 있다고 설명하고, 0이 아닌 숫자를 `true`로 변환하는 사례를 제시한다. 그러나 `reasonable without losing information`을 판정하는 별도의 정의나 알고리즘은 제공하지 않는다.

이 표현을 수학적인 무손실 변환으로 해석할 수는 없다. `1`, `2`, `42`를 모두 `true`로 변환하면 원래 숫자의 크기 정보는 사라지지만, 스펙은 이 변환을 허용 사례로 든다. 따라서 여기서의 기준은 원본 값을 복원할 수 있는지가 아니라, 내부 값이 의도한 Boolean 의미를 예측 가능하게 보존하는지에 가깝다.

JavaScript의 truthy/falsy 규칙을 그대로 적용해서도 안 된다. 문자열 `"false"`, 빈 문자열, 리스트, 객체의 참·거짓 해석은 언어와 구현에 따라 달라질 수 있으므로 GraphQL의 공통 계약이 될 수 없다.

### 오류 구분

리졸버가 반환한 내부 값을 `Boolean`으로 변환하지 못하면 실행 중 발생한 field error로 처리한다. 리졸버가 직접 반환하거나 던진 오류는 field error의 원인이 될 수 있지만, result coercion 실패처럼 리졸버가 성공한 뒤 발생하는 field error도 있다.

반면 잘못된 Boolean 입력은 실행 전 request error이며, 해당 요청의 리졸버를 실행하지 않는다.

## 실험 및 관찰

GraphQL.js 16의 기본 `Boolean` 구현은 다음과 같이 모호한 스펙 문구를 구체화한다.

| 내부 반환값 | 결과 |
| --- | --- |
| `true`, `false` | 그대로 반환 |
| 유한한 숫자 `0` | `false` |
| 유한한 0 이외의 숫자 | `true` |
| `NaN`, `Infinity` | 오류 |
| 문자열, 리스트, 객체 | 오류 |

이 정책은 범용 truthiness를 도입하지 않으면서 스펙에 명시된 숫자 변환 사례를 지원한다. 다만 참고 구현의 정책이지, 스펙이 모든 구현체에 강제하는 상세 알고리즘은 아니다.

## 결정 사항

- `reasonable without losing information`은 구현 가능한 형식 규칙으로 정의된 문구가 아니라 result coercion에 허용된 구현 재량으로 해석한다.
- NecrassRs에서 언어별 truthy/falsy 규칙을 일반화하지 않는다.
- 기본 `Boolean` result coercion은 상호 운용성과 예측 가능성을 위해 GraphQL.js와 같은 제한적 정책을 우선 검토한다. Boolean은 그대로 반환하고, 숫자를 허용한다면 유한한 값만 `0`과 비-`0`으로 변환하며 나머지는 field error로 처리한다.
- `Boolean` input coercion은 스펙대로 실제 Boolean 입력만 허용한다.

## 다음 작업

1. 주요 Rust GraphQL 구현체의 기본 `Boolean` result coercion 정책을 비교한다.
2. NecrassRs 스칼라 내부 값 표현을 정한 뒤 Boolean, Int, Float, String, ID 각각의 result coercion 표를 작성한다.
3. 허용값, 거부값, field error와 Non-Null 전파를 검증하는 최소 호환성 테스트를 추가한다.
