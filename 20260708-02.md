# 2026-07-08 개발일지 2

## 오늘의 목표

Pothos Drizzle 플러그인을 이용해 `User` GraphQL 타입과 `viewer` 쿼리를 구성하면서, GraphQL에서 타입 정의와 쿼리 진입점 정의가 서로 다른 작업이라는 점을 확인한다.

## 변경 사항

`necrassrs-lab`의 `User Query` 커밋에서 GraphQL 관련 구성이 분리되었다.

- `services/api-pothos-drizzle/src/graphql/builder.ts`
  - Pothos `SchemaBuilder` 생성
  - Drizzle plugin 설정
  - `GraphQLContext`와 `DrizzleRelations` 타입 연결
- `services/api-pothos-drizzle/src/graphql/context.ts`
  - resolver context에 `request`와 `db`를 포함하는 타입 정의
- `services/api-pothos-drizzle/src/graphql/user.ts`
  - Drizzle `users` 테이블 기반 `UserRef` 정의
  - `id`, `username`, `displayName`, `avatarUrl` 노출
  - `password`, `logged_in`은 GraphQL 타입에 노출하지 않음
- `services/api-pothos-drizzle/src/graphql/query.ts`
  - root `Query.viewer` 필드 정의
  - `x-user-id` 헤더를 읽고 `ctx.db.query.users.findFirst(...)`로 현재 사용자를 조회
- `services/api-pothos-drizzle/src/graphql/schema.ts`
  - `user.ts`, `query.ts`를 import한 뒤 `builder.toSchema()` 호출

`pnpm typecheck`는 통과했다.

## 구현 메모

이번에 혼동한 지점은 `User` 타입을 정의하는 것과 그 타입을 반환하는 쿼리를 정의하는 것이 별개의 작업이라는 점이었다.

`builder.drizzleObject("users", ...)`는 GraphQL schema 안에 `User` object type을 등록한다. 이 작업만으로는 클라이언트가 `User`를 조회할 수 없다. 클라이언트가 접근할 수 있는 root field가 필요하며, 이번 경우에는 `Query.viewer`가 그 역할을 한다.

또한 Pothos 기본 `t.field`와 Pothos Drizzle plugin의 `t.drizzleField`는 resolver 인자 형태가 다르다.

일반 Pothos field는 보통 다음 형태를 따른다.

```ts
resolve: (parent, args, ctx, info) => ...
```

반면 Drizzle plugin의 `t.drizzleField`는 selection-aware query helper를 첫 번째 인자로 받는다.

```ts
resolve: (query, root, args, ctx) => ...
```

`viewer`는 Drizzle-backed object를 반환하므로 `t.drizzleField`로 정의했고, resolver 안에서 `query(...)`를 `ctx.db.query.users.findFirst(...)`에 전달했다.

## 실험 및 관찰

처음에는 `UserRef`를 만들면 첫 번째 쿼리가 구현된 것으로 볼 수 있는지 혼동했다. 실제로는 `UserRef`는 타입 정의이고, `viewer`는 root Query의 필드 정의다. 두 작업이 모두 있어야 plan.md의 `viewer` 쿼리가 schema에 나타난다.

현재 구현은 `x-user-id` 헤더가 없으면 `null`을 반환하고, 헤더가 있으면 해당 ID의 user를 조회한다. 따라서 plan.md의 첫 번째 쿼리에서 요구한 "로그인 안 됨 → null, 로그인 됨 → 현재 User 반환"의 개발용 shortcut 버전이 구현되었다.

이번 혼동은 Pothos의 설계 문제라기보다 GraphQL schema 구성에 대한 이해 부족에서 비롯된 것으로 판단했다. GraphQL에서는 object type, root query field, resolver가 각각 다른 책임을 가진다. Pothos는 이 구분을 코드 구조로 드러낼 뿐이다.

## 결정 사항

`User` 타입 정의와 root query 정의를 파일로 분리하는 구조를 유지한다.

- `user.ts`: `UserRef`와 User field 정의
- `query.ts`: root Query field 정의
- `schema.ts`: 타입/쿼리 모듈을 import한 뒤 schema 생성

`viewer`는 Pothos Drizzle plugin의 selection 최적화를 활용하기 위해 `t.drizzleField`로 유지한다.

## 다음 작업

`viewer` 쿼리를 실제 HTTP 요청으로 검증한다. 특히 `x-user-id: usr_alice` 헤더가 있을 때 `viewer { id username displayName avatarUrl }`이 예상대로 반환되는지 확인한다.

그 다음 `login(username, password)` mutation을 추가해 `x-user-id` shortcut과 별개로 `users.logged_in` 기반의 실험용 인증 흐름을 구현한다.
