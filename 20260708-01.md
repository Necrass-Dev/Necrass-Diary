# 2026-07-08 개발일지 2

## 오늘의 목표

Pothos 기반 GraphQL 서버 실험을 시작하기 위한 패키지 구성을 정리하고, TypeScript 서버 파이프라인에서 발생하는 마찰을 기록한다. 이번 기록의 초점은 Pothos API 자체의 복잡도보다, Pothos를 실제 서버 프로젝트에 붙이기 위해 필요한 TypeScript 빌드 및 실행 환경이다.

## 변경 사항

`necrassrs-lab/services/api-pothos-drizzle/package.json`에 Pothos와 GraphQL Yoga가 추가되었다.

- `@pothos/core`
- `graphql-yoga`

이후 같은 서비스에 `tsdown`을 추가하고, `build` 스크립트를 `tsdown`으로 설정했다.

```json
{
  "scripts": {
    "build": "tsdown"
  }
}
```

현재 서비스는 Drizzle, better-sqlite3, Pothos, GraphQL Yoga, tsx, TypeScript, tsdown을 함께 사용하는 구조다.

## 구현 메모

Pothos 자체는 GraphQL schema builder로서 비교적 단순하게 붙일 수 있는 편이다. 핵심 의존성은 `@pothos/core`이고, HTTP 서버 런타임으로는 `graphql-yoga`를 사용한다. 이 조합만 보면 GraphQL 서버 구성 자체는 크게 복잡하지 않다.

실제 마찰은 TypeScript 프로젝트를 서버로 실행하고 빌드하는 파이프라인에서 나온다. 개발 중에는 `tsx`로 TypeScript 파일을 직접 실행하고, 배포 또는 산출물 확인을 위해서는 `tsdown`으로 빌드하는 흐름을 잡아야 한다. `tsdown`은 설정 부담을 많이 줄여주지만, TypeScript 모듈 해석, ESM 설정, 빌드 스크립트, 실행 스크립트, 산출물 관리 같은 문제를 완전히 없애지는 않는다.

## 실험 및 관찰

`tsdown`을 추가하면서 빌드 스크립트 자체는 간단해졌다. 별도 번들러 설정 파일 없이 `package.json`에서 `build: tsdown`만으로 시작할 수 있다는 점은 장점이다.

다만 Pothos의 사용성이 단순하더라도 TypeScript 기반 서버 실험은 여러 도구가 함께 필요하다. Drizzle migration과 seed는 `tsx`로 실행하고, 서버 코드는 Pothos 및 GraphQL Yoga와 연결해야 하며, 빌드는 다시 `tsdown`을 거친다. 따라서 프레임워크 API의 단순함과 프로젝트 파이프라인의 단순함은 별개의 문제로 봐야 한다.

이번 관찰은 "Pothos 자체는 단순한데, tsdown 같은 파이프라인이 조금 귀찮다"로 정리할 수 있다. `tsdown`은 불편을 줄여주지만, TypeScript 서버 개발의 도구 체인 자체가 사라지는 것은 아니다.

## 결정 사항

Pothos 실험은 계속 진행한다. 다만 평가 기준에는 schema builder API뿐 아니라 TypeScript 빌드 및 실행 파이프라인의 부담도 포함한다.

`tsdown`은 현재 단계에서 빌드 도구로 유지한다. 직접 번들러 설정을 늘리기보다는, 기본값으로 시작해 실제 서버 코드가 붙은 뒤 필요한 설정만 추가하는 방향이 적절하다.

## 다음 작업

Pothos와 GraphQL Yoga를 실제 서버 엔트리포인트에 연결한다. 우선 `login`, `viewer`, `project(key:)`, `project.issues`, `node(id:)`를 구현하고, 이후 mutation과 issue detail을 추가한다.

서버 구현이 진행되면 `pnpm build` 결과와 개발 실행 흐름을 함께 확인한다. 특히 `tsx` 개발 실행과 `tsdown` 빌드 산출물 사이에서 모듈 경로, ESM, 실행 파일 위치 문제가 생기는지 기록한다.
