# 2026-09-17 NecrassRs 개발 기록

## 개요

NecrassRs는 GraphQL SDL을 공개 API 계약의 기준으로 삼고 Rust 타입, resolver 계약과 실행 연결 코드를 생성하는 서버 프레임워크다. 사용자는 생성된 계약에 맞춰 별도 Rust 코드에 비즈니스 로직을 작성한다.

이 글은 7월부터 9월 17일까지의 비교 실험과 설계 논의를 종합한다. 본 구현의 현재 계획은 다음과 같다.

- `*.graphql`을 공개 API의 유일한 기준으로 사용한다.
- `apollo-parser`를 사용한다. `apollo-compiler` 도입과 실행 엔진 선택은 보류한다.
- `necrass init`으로 프로젝트를 준비하고, 코드 생성은 `build.rs`로 통합한다. `necrass compile`은 제거한다.
- 모든 출력 필드를 resolver로 취급하고, 사용자 구현체를 생성된 wrapper struct로 감싼다.
- 미구현 골격은 `unimplemented!()`를 사용한다. 미완성 상태에서도 빌드하고 완성한 부분부터 테스트한다.
- 미구현 필드가 호출되면 GraphQL 오류로 복구하지 않고 서버 프로세스를 종료한다.
- 자동 생성 코드와 사용자 구현을 분리하고 기존 업무 로직을 보존한다.
- 프로토타입에서는 검증 결과만 참고하며 코드는 재사용하지 않는다.

이 계획은 본 구현의 완료 보고가 아니다. 프로토타입에서 검증한 범위와 새로 합의한 정책을 구분한다.

## 배경

### 비교 실험에서 확인한 반복

Pothos·GraphQL Yoga·Drizzle과 async-graphql·Axum·SQLx로 같은 이슈 관리 API를 구현했다. 비교 기준은 기능의 존재 여부보다 계약의 위치와 필드 추가 시 반복되는 선언이었다.

Pothos는 builder 호출로 객체, root field와 relation을 구분하고 Drizzle 메타데이터를 연결했다. 반면 개발 실행, migration, seed, ESM 및 빌드에는 TypeScript 도구 체인 구성이 필요했다. 객체 타입을 등록하는 것과 root query를 만드는 것은 별개의 작업이었다.

Rust 실험에서는 DB row, GraphQL 객체, 변환 함수, 필드 메서드, root 등록에 같은 정보가 분산됐다. Context 주입, 관계 조회, Node 분기, connection과 Mutation에서도 반복이 누적됐다. 이는 기존 프레임워크에 단순 getter나 DataLoader가 없다는 뜻이 아니라, 실험에서 선택한 조합과 구현 방식의 비용이다. 초기 조사에 포함된 외부 성능 사례도 프로젝트 자체의 측정 결과와 구분한다.

### GraphQL과 Rust의 표현 차이

리스트와 nullability는 구조적으로 표현해야 한다. 입력은 생략, 명시적 null, 값을 구분해야 하며, interface와 union에는 실행 결과의 실제 객체를 표현할 수단이 필요하다.

9월 15일에는 input coercion과 result coercion을 구분했다. 클라이언트 입력의 허용 규칙과 내부 반환값 변환 정책은 별개이며, 언어의 truthy/falsy 규칙을 공통 계약으로 삼지 않는다. 구체적인 스칼라 변환 정책은 내부 값 표현과 실행 엔진을 정한 뒤 검증한다.

## 구현 과정

### 기준 API와 Rust 이식

Pothos 실험에서는 viewer, organization, project, issues connection과 여러 Mutation을 구성했다. Rust 실험은 별도 SQLite DB와 migration·seed를 사용하고, HTTP 헤더에서 추출한 개발용 사용자 정보를 요청 Context에 주입했다. DB 내부 컬럼을 노출하지 않도록 row와 GraphQL 객체를 분리했다.

User에서 Organization, Issue, Project, Comment로 확장하며 다음 비용을 관찰했다.

- 관계 필드에는 외래 키 보관, SQL, 결과 변환과 loader 연결이 필요했다.
- connection에는 cursor 해석 외에도 keyset 조건, 정렬, 추가 한 건 조회와 pageInfo 조립이 필요했다.
- `node(id:)`는 타입별 조회·변환 분기를 요구했고 새 타입 추가가 여러 파일에 영향을 주었다.
- Mutation은 입력·payload 선언, 인증, ID 및 존재 확인, SQL과 root 등록을 반복했다.
- `updateIssue`는 `MaybeUndefined`로 생략·null·값을 구분했다.
- `addComment`는 댓글 삽입과 이슈 갱신을 하나의 트랜잭션으로 묶었다.

범용 repository나 factory로 반복을 감추지는 않았다. 특히 7월 Mutation 단계의 검증은 컴파일과 형식 검사였으며 전체 API 동작 검증을 의미하지 않는다.

### 설계의 변화

초기에는 DB mapping, Node 조회, relation과 batching 자동화를 검토했다. 첫 구현 목표는 SDL 계약을 Rust 계약과 실행 연결 코드로 옮기는 것으로 좁혔다. SQL 생성, ORM 통합과 자동 batching은 현재 범위에 포함하지 않는다.

Tonic에서 계약 파일·생성 코드·사용자 구현을 분리하는 접근을 가져왔다. 단순 필드의 자동 getter를 제공하는 A안도 검토했지만, 인자 유무로 권한이나 Context 의존성을 판단할 수 없어 모든 출력 필드를 resolver로 다루는 C안을 선택했다. 추가 resolver 편의를 위한 B안은 검토 이력으로 남기고 별도 추가 골격 기능은 당장 제공하지 않는다.

9월 16일에는 독립 생성 명령인 `necrass compile`과 `--watch`를 검토했다. 9월 17일 최종 논의에서는 이를 제거하고 생성 진입점을 `build.rs`로 통합했다. 미완성 골격의 `compile_error!()`도 `unimplemented!()`로 변경했다. 모든 필드를 완성한 뒤 실행하는 방식보다, 완성한 필드부터 실행·테스트하는 개발 흐름을 우선한 결정이다.

## 실험에서 확인한 점

별도 저장소 `necrassrs-prototype`에서 `Query.user → User.name`을 시작으로 계약과 실행 연결을 검증했다. 초기에는 async-graphql dynamic-schema를 사용했고, 이후 Apollo Compiler 1.32.0을 이용한 검증·생성·실행 실험으로 확장했다.

- 사용자 struct를 wrapper에 보관하고 서로 다른 구현체를 같은 출력 타입으로 반환할 수 있었다.
- 공개 trait의 Send future와 내부 boxed future를 연결하고 사용자는 `async fn`으로 구현할 수 있었다.
- Cargo 빌드로 SDL에서 trait·wrapper·실행 dispatch를 생성했다.
- 기본 스칼라, 리스트·nullability, enum, 재귀 input object, 기본값과 입력 삼중 상태를 검증했다.
- interface 상속, union, fragment, custom scalar 변환과 Mutation 순서를 실험했다.
- 여러 SDL 파일의 병합·추가·삭제, 이름 충돌 진단, 계약 변경의 컴파일 오류와 사용자 파일 보존을 확인했다.

커밋 `df01497` 기준으로 알려진 실패를 제외한 테스트 9개, 빌드와 Clippy가 통과했다. 전체 테스트는 아래 Apollo 재현 테스트 한 건 때문에 실패한다. custom scalar 입력을 요청 사전 검증이 아닌 필드 호출 경계에서 변환한 점은 프로토타입 어댑터의 제한이다.

### 실행기 문제의 기록

초기 async-graphql 7.2.1 dynamic-schema 실험에서는 resolver 오류 경로가 비어 있는 현상을 기록했다. 이후 Apollo 기반 실행에서는 해당 경로 테스트가 통과했지만, nullable 리스트 항목의 오류 전파 문제가 발견됐다.

Apollo 실험에서 `[Token]!`, `[Float]!`의 두 번째 항목 변환을 실패시키면 기대 결과는 해당 항목만 null인 리스트다. 실제로는 `data: null`이 반환됐다. 로컬 소스의 `complete_list_value`가 스트림 항목 오류를 항목 타입의 `try_nullify`에 전달하기 전에 전파하는 경로를 확인했다.

의존 실행기를 수정하거나 우회하지 않고 올바른 기대값의 실패 테스트와 원인만 보존했다. 이는 해당 버전과 실행 경로에서의 관찰이며 다른 버전에 일반화하지 않는다. 프로토타입은 폐기 대상이며, 이 결과가 본 구현의 Apollo Compiler 채택을 의미하지 않는다.

## 설계 결정

### 1. 개발 흐름

1. `necrass init`으로 초기 SDL, Cargo 설정, `build.rs`, 사용자 구현 모듈과 서버 진입점을 준비한다.
2. 사용자가 SDL에 타입, 필드, 인자와 nullability를 정의한다.
3. `cargo build`가 `build.rs`를 통해 SDL 검증, Rust 코드 생성과 컴파일을 수행한다.
4. 사용자가 resolver 골격의 `unimplemented!()`를 실제 구현으로 교체한다.
5. `cargo run`으로 서버를 실행하고 완성한 필드부터 테스트한다.

`necrass compile`과 `necrass compile --watch`는 제공하지 않는다. 독립 watch 기능은 현재 범위에서 제외한다. 생성기 호출은 `build.rs`를 기준으로 통합한다.

### 2. 책임 레이어

| 레이어 | 책임 |
| --- | --- |
| SDL 파서 | `apollo-parser`로 SDL 파싱과 문법 오류 진단 |
| 의미 검증·IR | 스키마 규칙 검증, 코드 생성에 필요한 내부 표현 구성 |
| Rust 생성기 | 타입, resolver trait, wrapper, 호출 어댑터와 구현 골격 생성 |
| 실행 엔진 | 요청 검증, 입력 처리, 선택 필드 실행, 오류와 null 전파 |
| HTTP·테스트 UI | 요청 Context 연결, GraphQL 엔드포인트와 브라우저 쿼리 테스트 화면 제공 |
| CLI·빌드 통합 | 프로젝트 초기화와 Cargo 생성·컴파일 연동 |

`apollo-parser` 사용은 확정한다. 파싱 성공과 스키마 의미 검증 성공은 구분하며, `apollo-compiler` 도입과 실행 엔진 선택은 추후 결정한다. 레이어 구분이 곧 별도 crate 구성을 뜻하지는 않는다.

IR은 타입·필드·인자·기본값, 리스트·nullability, 추상 타입 관계와 GraphQL/Rust 이름 대응을 담는다. 업무 로직이나 DB 조회 정책을 추측하지 않는다. Yoga 연동이라는 초기 표현은 JavaScript 서버 런타임 연결이 아니라 브라우저용 테스트 UI 제공을 의미한다.

### 3. 타입과 resolver 계약

모든 출력 필드는 resolver로 취급한다. `id`, `name` 같은 단순 필드도 예외가 아니다. SDL 인자는 클라이언트 입력이며, Context는 인증 정보·헤더에서 추출한 값·DB 접근 등 서버 측 요청 정보를 전달한다.

GraphQL 객체는 생성된 Rust wrapper struct로 표현한다. 사용자 struct가 상태와 업무 로직을 보유하고 resolver trait을 구현한다. 상위 resolver는 이를 wrapper로 감싸 반환하며, wrapper 생성만으로 하위 필드를 실행하지 않는다. 실행 엔진이 선택한 필드만 호출한다.

입력의 생략·명시적 null·값을 보존하고 리스트와 항목의 nullability를 구분한다. input object와 enum은 데이터 타입으로 생성한다. Box/Arc, Send/Sync, 수명, 인자 묶음과 오류 타입의 최종 공개 API는 프로토타입의 선택으로 고정하지 않는다.

### 4. 코드 소유권과 SDL 변경

자동 재생성하는 계약·wrapper·실행 연결 코드는 `OUT_DIR`, 사용자 구현은 `src`에 둔다. Rust에서 SDL로 역동기화하지 않으며, 기존 사용자 구현을 임의로 덮어쓰거나 삭제하지 않는다.

SDL 필드 삭제 시 생성 계약과 실행 연결에서 해당 필드를 제거한다. 기존 trait impl에 남은 메서드나 변경된 시그니처의 불일치는 컴파일러로 검출한다. 사용자는 제거된 API의 업무 코드를 삭제하거나 일반 함수로 옮길 수 있다.

최초 및 추가 필드의 편집용 골격을 제공하는 방식은 미정이다. `OUT_DIR`의 파일을 직접 채우게 하면 재생성 때 구현이 사라지므로 다음 대안을 구분해 검토한다.

- 사용자 impl에 없는 골격만 안전하게 추가한다.
- 생성된 trait에 미구현 기본 메서드를 제공하고 사용자는 필요한 메서드를 구현한다.

후자는 모든 메서드를 impl에 명시하도록 요구했던 계약을 바꾼다. `unimplemented!()` 채택만으로 기본 메서드 제공까지 확정한 것은 아니다. 별도 생성 명령 없이 점진적 개발이 가능하면서도 사용자 코드를 보호하는 방식을 결정해야 한다.

### 5. 미구현과 panic 정책

미구현 골격은 `unimplemented!()`를 사용한다. 본문이 미완성이어도 컴파일을 허용하고 해당 필드를 호출하지 않는 테스트를 진행할 수 있게 한다. 잘못된 SDL이나 Rust 타입 불일치는 계속 빌드 오류로 처리한다. 메서드 본문의 미구현과 trait 메서드 자체의 누락은 다른 문제이며, 후자는 골격 제공 정책에서 해결한다.

미구현 필드가 호출되면 panic을 발생시키고 서버 프로세스를 종료한다. 이를 GraphQL 오류로 변환하거나 요청 단위로 복구하지 않는다. 정상적인 도메인 오류의 GraphQL 처리와는 구분한다.

비동기 태스크의 panic만으로 프로세스 종료를 보장한다고 가정하지 않는다. 서버 실행 바이너리는 `panic = "abort"`와 같은 프로세스 종료 정책을 적용하는 방향으로 구성한다. 해당 설정은 미구현 필드뿐 아니라 모든 panic에 적용된다. 구체적인 Cargo profile 적용과 실제 종료 검증은 본 구현에서 수행한다.

## 남은 과제

1. 최초·추가 resolver 골격의 제공 방식을 결정한다. 사용자 파일 보호와 부분 구현 상태의 빌드 가능성을 함께 만족해야 한다.
2. `apollo-parser` 출력에서 최소 IR로 변환하는 경계와 의미 검증 방식을 정한다. `apollo-compiler` 필요성은 이 단계에서 판단한다.
3. Context, 인자, 반환값 소유권, 비동기 호출 및 오류 타입의 공개 계약을 확정한다.
4. 실행 엔진과 목표 GraphQL 명세 판본, 호환성 검증 범위 및 타입별 지원 순서를 정한다.
5. 빌드 재생성, 사용자 구현 보존, 미구현 필드 미호출 시 정상 실행과 호출 시 프로세스 종료를 검증한다. 종료 검증은 별도 프로세스에서 수행한다.
6. 실행 엔진과 분리된 HTTP 어댑터 및 개발용 테스트 UI를 설계한다.

SQL 생성, ORM 통합, 자동 batching과 성능 최적화는 현재 계약·생성·실행 흐름을 정한 이후 필요성을 판단한다. 이번 작업에서는 Article만 개정하며 프로토타입을 수정하지 않는다.

## 기록 근거

20260708-01·02, 20260709-01, 20260710-1~8, 20260722-1·2, 20260726-1~6 및 20260726 누적 Article, 20260914-1, 20260915-1, 20260916-1·2와 기존 20260917 Article을 종합했다. 프로토타입 실험 결과와 9월 17일 최종 설계 대화를 추가 반영했다. 이전 일지는 당시의 결정 이력으로 보존하며, 충돌하는 사항은 이 글의 최신 설계 결정을 따른다.
