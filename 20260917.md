# 2026-09-17 NecrassRs 개발 기록

## 개요

NecrassRs는 GraphQL SDL을 공개 API 계약의 기준으로 삼고, Rust 타입·resolver trait·실행 연결 코드를 생성하는 서버 프레임워크를 목표로 한다. 사용자는 생성 파일을 수정하는 대신 별도 Rust 코드에서 resolver trait을 구현한다. Tonic의 계약 파일, 생성 코드, 사용자 구현 분리에서 가져온 접근법이다.

이 글은 2026년 7월부터 9월 17일까지의 개발일지와 오늘의 설계 논의를 종합한다. 비교 실험에서 관찰한 사실, 현재 채택한 설계, 아직 검증하지 않은 가설을 구분한다. 오늘까지 합의한 CLI와 생성기는 구현 완료된 기능이 아니다.

현재의 핵심 결정은 다음과 같다.

- `*.graphql`을 유일한 공개 API 계약으로 유지한다.
- 모든 출력 필드를 사용자 resolver로 구현하는 C안을 채택한다.
- GraphQL 객체는 사용자 resolver 구현체를 보관하는 생성된 Rust wrapper struct로 표현한다.
- `cargo build`만으로 최신 SDL의 코드 생성과 Rust 컴파일이 가능해야 한다.
- `necrass compile`은 Rust 구현이 미완성인 상태에서도 사용할 수 있는 독립적인 생성 명령이다.
- 사용자 소스는 재생성 과정에서 수정하거나 삭제하지 않는다.
- 생성기를 만들기 전에 `Query.user → User.name` 실행 경로를 수작업 코드로 검증한다.

## 배경

### 비교 실험에서 출발한 문제

Pothos·GraphQL Yoga·Drizzle을 사용한 TypeScript 실험과 async-graphql·Axum·SQLx를 사용한 Rust 실험에서 같은 이슈 관리 API를 작성했다. 관심사는 기능의 존재 여부보다 계약이 어디에 정의되는지, 필드를 추가할 때 어떤 정보를 반복 작성하는지였다.

Pothos의 builder는 타입, root field, relation을 명시적으로 구분했다. Drizzle 플러그인은 DB 메타데이터를 활용해 단순 필드와 관계를 연결했다. 반면 TypeScript 실행·빌드에는 tsx, tsdown, ESM 설정 등 별도의 파이프라인 구성이 필요했다.

async-graphql·SQLx 실험에서는 DB row, GraphQL 객체, row 변환, 필드 메서드, root 등록에 같은 정보가 분산되었다. 매크로 기반 선언에서는 실제 공개 계약을 이해하기 위해 Rust 타입과 attribute, root 조립을 함께 읽어야 했다. Context 전달, 관계 조회, Node 분기, connection, Mutation에서도 반복이 누적되었다.

이는 기존 프레임워크에 단순 getter나 DataLoader 기능이 없다는 뜻은 아니다. 이전 실험에서 선택한 조합과 작성 방식의 비용이며, 기존 기능을 활용한 구현과도 비교해야 한다. 컴파일 시간이나 성능에 대한 초기 조사 사례 역시 이 프로젝트에서 재현한 측정 결과와 구분한다.

### Rust 표현과 GraphQL 계약의 차이

GraphQL 타입의 list·nullability, 입력값의 생략·명시적 null·값 전달, interface·union은 Rust 타입에 기계적으로 일대일 대응하지 않는다. 특히 부분 갱신 입력에서 생략과 null을 합치면 의미가 달라진다. 내부 표현과 생성 시그니처가 이 차이를 보존해야 한다.

9월 15일에는 Boolean의 input coercion과 result coercion을 구분했다. 입력 허용 규칙과 내부 반환값 변환 정책은 별개의 문제이며, 언어의 truthy/falsy 규칙을 그대로 공통 계약으로 삼지 않기로 했다. 숫자 반환값의 허용 여부 등 구체적인 정책은 실행 엔진과 내부 값 표현을 정한 뒤 검증할 과제로 남아 있다.

## 구현 과정

### 기준 API와 Rust 비교 구현

Pothos 실험에서는 viewer, organization, project, issues connection과 여러 Mutation을 구성했다. Rust 실험은 별도 SQLite DB와 migration·seed를 사용했고, HTTP 헤더에서 얻은 개발용 사용자 정보를 요청 Context에 주입했다. DB 내부 컬럼을 공개 API에 노출하지 않도록 row와 GraphQL 객체를 분리했다.

User에서 Organization, Issue, Project, Comment로 확장하며 다음 비용을 관찰했다.

- 단순 필드도 row·출력 객체·변환·메서드에 걸쳐 반복되었다.
- 관계 필드는 parent의 외래 키, SQL, 결과 변환, loader 연결을 요구했다.
- `Project.issues`는 cursor decoding 외에도 keyset 조건, 정렬, 추가 한 건 조회, pageInfo 조립을 요구했다.
- `node(id:)`는 타입별 조회와 변환 분기가 필요했고 타입 추가 시 여러 파일을 수정했다.
- Mutation은 입력·payload 선언, 인증, ID 검증, 존재 확인, SQL, root 등록을 반복했다.
- `updateIssue`에서 `MaybeUndefined`로 생략·null·값을 구분했다.
- `addComment`에서는 댓글 삽입과 이슈 갱신을 트랜잭션으로 묶었다.

각 시점의 일지는 컴파일 실패와 복구, formatter·compiler 검사 결과를 기록한다. 특히 7월 Mutation 단계의 검증은 컴파일과 형식 검사였으며, 전체 API의 동작 테스트 완료를 의미하지 않는다. 이 글은 기존 일지를 종합한 것으로, 이후의 lab 변경 전체를 새로 재검증한 보고서는 아니다.

### 설계 범위의 축소와 변경

초기에는 DB mapping, Node 조회, relation 및 batching 자동화를 폭넓게 검토했다. 현재 첫 목표는 SDL 계약을 Rust 계약과 실행 연결 코드로 옮기는 것이다. SQL 생성, ORM 통합, 실행기 수준의 자동 batching은 이번 최소 검증에 포함하지 않는다.

9월 14일에는 Cargo 빌드 중심의 생성 흐름을 정했다. 이후 일회성 생성의 필요성을 반영해 `necrass compile`을 추가했고, 별도 `necrass watch` 대신 `necrass compile --watch`로 정리했다. 이는 Cargo 단독 빌드 원칙을 대체하지 않는다.

단순 필드를 자동 getter로 제공하는 A안도 검토했지만, 현재는 모든 출력 필드를 사용자 resolver로 구현하는 C안을 선택했다. 인자 유무만으로 기본 필드와 사용자 resolver를 나눌 수 없으며, 단순 필드도 요청 Context나 권한에 의존할 수 있다. SDL 외부의 필드 분류 설정을 먼저 도입하지 않고 일관된 계약으로 시작한다. 그 대가로 단순 getter 구현은 남는다. 초기 문제의 모든 반복을 한 번에 제거했다고 주장하지 않는다.

추가 resolver 편의를 위한 B안은 검토 이력으로 남기되, 추가 구현 골격을 자동으로 제공하는 기능은 현재 범위에서 제외한다.

## 실험에서 확인한 점

기존 기록으로 확인한 것은 계약 선언과 DB 연결의 반복, 입력 삼중 상태의 필요, connection과 Mutation의 구현 비용이다. 코드 생성만으로 DB 접근 정책이나 N+1을 해결할 수 있다는 결과는 아직 없다.

다음 사항은 오늘 설계를 정리할 때의 검증 목표다. 이후 수행한 첫 실험 결과는 이 글 마지막의 검증 결과에 별도로 기록한다.

- 생성된 wrapper가 서로 다른 resolver 구현체를 같은 Rust 객체 타입으로 감쌀 수 있는가.
- 사용하기 쉬운 비동기 trait과 내부 타입 소거를 함께 제공할 수 있는가.
- 기존 실행 엔진에 연결했을 때 선택된 필드만 실행되고 오류·null 전파가 유지되는가.
- 필수 구현 누락과 SDL 변경이 의도한 컴파일 오류로 드러나는가.
- CLI와 Cargo가 동일한 계약을 생성하면서 사용자 파일을 보존하는가.

## 설계 결정

### 1. SDL First와 사용자 코드 보존

`*.graphql`이 타입, 필드, 인자, nullability의 기준이다. Rust에서 SDL로 역동기화하지 않는다. SDL 변경 시 생성 계약과 등록 코드를 갱신하고, Rust 컴파일러로 사용자 구현과의 불일치를 검사한다.

삭제된 SDL 필드의 사용자 코드를 자동 삭제하지 않는다. 기존 trait impl에 제거된 메서드가 남으면 오류로 드러나지만, 별도 일반 함수로 옮긴 코드까지 오류가 나야 하는 것은 아니다. 공개 API에서 제거하는 것과 업무 코드를 삭제하는 것은 다른 작업이다.

### 2. CLI와 Cargo의 역할

| 진입점 | 책임 |
| --- | --- |
| `necrass init` | 최초 SDL, Cargo 설정, build.rs, 서버 진입점 준비 |
| `necrass compile` | SDL 검증과 Rust 계약·연결 코드의 일회성 생성 |
| `necrass compile --watch` | SDL 변경 감지와 같은 생성 작업의 반복 |
| `cargo check` / `cargo build` | build.rs로 최신 SDL을 반영하고 Rust 구현 검사·컴파일 |
| `cargo run` | 생성과 빌드를 거쳐 서버 실행 |

`necrass compile`은 Rust 구현이 미완성인 상태에서 SDL을 많이 변경할 때도 실행 가능해야 한다. 생성 성공은 사용자 구현의 컴파일 성공과 다르다. watch 역시 생성 책임을 맡으며 서버 자동 재시작은 현재 범위가 아니다.

CLI와 build.rs는 같은 생성기를 사용한다. `necrass compile`은 필수 선행 명령이 아니다. Cargo 빌드는 사용자 구현 파일을 생성·수정·삭제하지 않으며, 구현이 없으면 생성 이후 Rust 컴파일이 실패한다.

Cargo 생성물은 OUT_DIR을 사용하는 방향이다. 독립 CLI의 출력 위치 및 프로젝트 연결 방식은 미정이다. 최초 사용자 impl 골격은 CLI에서 제공하되 정확한 최초 실행 판정과 파일 배치는 추가 설계한다.

### 3. 모든 출력 필드의 resolver 계약

id와 name 같은 단순 필드도 사용자 resolver로 구현한다. input object와 enum 등은 데이터 타입으로 생성한다. SDL 인자는 클라이언트가 전달하는 값이고 Context는 인증·헤더에서 추출한 정보·서버 서비스 등 요청 실행 환경이다. 헤더 정보를 SDL 인자로 취급하지 않는다.

필수 trait 메서드에는 미구현 기본 동작을 제공하지 않는다. 최초 impl 골격의 미완성 부분에는 `todo!()` 대신 `compile_error!()`를 사용한다. 구현 누락과 미완성 골격을 컴파일 단계에서 드러내되, 컴파일 성공이 도메인 로직의 정확성을 보장한다고 보지는 않는다.

SDL 변경 후 새 메서드를 사용자 소스에 자동 삽입하는 기능은 당장 만들지 않는다. 사용자는 갱신된 trait과 컴파일 오류를 보고 구현을 수정한다.

### 4. 생성된 객체 wrapper와 사용자 struct

GraphQL User는 생성된 Rust User struct에 대응한다. User는 사용자의 UserResolver 구현체를 보관한다. 실제 데이터와 내부 DB 키는 사용자의 UserHandler 같은 struct에 두며, 해당 struct가 필드 메서드를 구현한다.

상위 resolver는 `User::new(handler)`로 감싼 값을 반환한다. wrapper 생성 시 모든 필드를 미리 실행하지 않는다. 실행 엔진이 선택된 필드를 요청할 때 사용자 메서드를 호출한다. User의 대응 타입을 연관 타입으로 사용자가 지정하는 방식은 채택하지 않는다.

내부 저장 방식, Box와 Arc의 선택, 비동기 dispatch, Send/Sync 및 수명 제약은 검증 전이다. native async trait을 그대로 dyn으로 사용할 수 있다고 가정하지 않는다.

### 5. 책임 레이어

| 레이어 | 책임 |
| --- | --- |
| SDL 파서와 검증 | 기존 파서 재사용, 문법 및 스키마 유효성 확인 |
| 최소 IR | 코드 생성에 필요한 타입·필드·인자·타입 참조 정리 |
| Rust 생성기 | 데이터 타입, 객체 wrapper, resolver trait, 등록·연결 코드 생성 |
| GraphQL 실행 계층 | 요청 검증, 입력 처리, 필드 실행, 오류 및 null 전파 |
| HTTP와 개발 UI | GraphQL 엔드포인트와 Playground/GraphiQL 제공 |
| CLI와 Cargo 연동 | 초기화, 명시적 생성, 변경 감지, 빌드 연동 |

책임 구분이 곧 별도 crate 구성을 뜻하지 않는다. IR은 스키마의 중간 표현이고 resolver는 실행 시 사용자 구현을 호출하는 계약이다. 기존 AST를 불필요하게 복제하지 않으며 Rust AST 구성 라이브러리 역시 필요를 확인한 뒤 선택한다.

실행 엔진은 우선 기존 구현을 활용한다. Yoga 연동이라는 표현은 JavaScript 서버 런타임과의 연결이 아니라 Rust 서버에서 브라우저용 테스트 UI를 제공한다는 의미로 정정했다.

### 6. 최소 실행 검증

다음 SDL 하나를 대상으로 생성될 코드를 먼저 수작업으로 작성한다.

```graphql
type Query {
  user(id: ID!): User
}

type User {
  name: String!
}
```

계약 초안은 다음과 같다. Context, ID, 오류 타입 및 비동기 표현은 아직 확정된 공개 API가 아니다.

```rust
pub trait QueryResolver {
    async fn user(&self, ctx: &Context, id: ID)
        -> ResolverResult<Option<User>>;
}

pub trait UserResolver {
    async fn name(&self, ctx: &Context) -> ResolverResult<String>;
}
```

첫 실험은 모든 필드를 비동기로 호출하고, Context를 전달하며, SDL 인자는 개별 인자로 받고, 반환값은 소유한 값으로 통일하는 초안을 검증한다. 이는 최종 API 확정과 구분한다.

사용자는 QueryHandler와 UserHandler에 trait을 구현한다. QueryHandler는 ID가 1이면 `User::new(UserHandler { ... })`를 반환하고 없으면 None을 반환한다. 이름 예시 값은 타치바나 셰리로 한다. DB, HTTP, CLI, watch, 생성기는 이 실험에 필요하지 않다.

실행 엔진은 요청 검증과 인자 해석, 선택 필드 실행, 결과 조립과 오류 처리를 맡는다. 생성될 연결 코드는 인자를 Rust 값으로 전달하고 사용자 resolver를 호출한다. GraphQL 실행 의미론을 연결 코드에서 재구현하지 않는다.

성공 조건은 다음과 같다.

1. `{ user(id: "1") { name } }`이 기대한 이름을 반환한다.
2. 없는 ID는 errors 없이 `user: null`을 반환한다.
3. 선택하지 않은 name resolver는 호출되지 않는다.
4. name 실행 실패 시 오류가 보고되고 nullable 부모 user까지 null이 전파된다.
5. UserResolver의 name 구현을 제거하면 Rust 컴파일이 실패한다.

이 경로를 통과한 코드에서 반복되는 부분을 추출해 최소 IR과 생성기 출력을 정한다. 수작업 연결 코드의 성공을 SDL 코드 생성의 성공으로 혼동하지 않는다.

## 남은 과제

1. 독립된 로컬 검증 저장소에서 위 실행 경로를 구현하고 테스트한다.
2. wrapper의 저장·비동기 호출 방식과 실행 엔진 연동을 함께 검증한다.
3. Context, 인자, 오류, 반환값 소유권의 정확한 공개 계약을 정한다.
4. nullable, 입력 생략, custom scalar, interface·union 및 subscription의 표현과 지원 순서를 정한다.
5. 파서와 의미 검증의 재사용 범위, 최소 IR과 이름 충돌 진단을 정한다.
6. CLI와 Cargo 출력 경로, 최초 골격 생성 조건과 파일 보호 규칙을 정한다.
7. SDL 추가·삭제·변경에 대한 컴파일 오류와 사용자 파일 보존을 검증한다.
8. 목표 GraphQL 명세 판본과 호환성 테스트 범위를 명시한다.

성능 최적화, SQL 생성, 자동 batching, 추가 impl 골격 삽입은 현재 최소 실험을 통과한 뒤 필요성을 다시 판단한다.

## 기록 근거

20260708-01·02, 20260709-01, 20260710-1~8, 20260722-1·2, 20260726-1~6 및 20260726 누적 Article, 20260914-1, 20260915-1, 20260916-1·2를 읽고 종합했다. 최신 C안, wrapper struct, 미완성 골격의 compile_error!, 최소 실행 검증 계획은 9월 17일 대화의 합의를 반영한다. 이전 기록과 충돌하는 경우 본문의 최신 결정을 현재 계획으로 사용한다.

## Article 작성 이후의 첫 검증 결과

별도 로컬 Git 저장소 `necrassrs-prototype`을 만들고 계약과 연결 코드를 수작업으로 작성했다. 기존 lab에서 사용하는 async-graphql 7.2.1의 dynamic-schema를 실험용 엔진으로 재사용했다. SDL 파서·IR·생성기·CLI·HTTP는 아직 구현하지 않았다.

User wrapper에는 Box로 타입을 소거한 사용자 구현체를 보관했다. 공개 trait은 Send future 반환을 명시하고 사용자 구현에서는 async fn을 사용했다. 내부 어댑터가 boxed future로 변환한다. 서로 다른 정상·오류 구현체를 같은 User로 반환할 수 있었으며, 이 구조는 컴파일되었다. Box 선택, Send/Sync/'static 제약과 엔진 ID·오류 타입 재사용은 실험 선택이며 최종 공개 API 결정은 아니다.

`cargo test --locked -- --skip resolver_error_path`에서 실행 테스트 1개와 구현 누락 compile-fail doctest 1개가 통과했다. 정상 조회, 없는 ID의 null, 미선택 name의 호출 생략, name 오류의 부모 null 전파, 잘못된 인자의 실행 전 거부, 요청 Context 전달을 확인했다.

단, 전체 `cargo test --locked`는 resolver_error_path 테스트가 실패했다. resolver 오류의 path가 `["user", "name"]`이 아니라 비어 있었다. 이 결과는 필드 실행 오류의 응답 경로를 요구하는 [GraphQL 오류 형식](https://spec.graphql.org/October2021/#sec-Errors.Error-result-format)에 맞지 않는다. 로컬 엔진 소스의 dynamic/resolve.rs에서 resolver 오류 변환 시 경로를 설정하지 않는 지점을 확인했다. 다른 버전이나 정적 실행 경로의 문제로 일반화하지 않는다.

실패 테스트를 제거하거나 기대값을 낮추지 않고 재현 자료로 남겼다. 기존 엔진을 사용해도 호환성 검증이 필요하다는 구체적인 결과다. 다음 검증은 이 오류 경로를 wrapper와 무관한 엔진 최소 예제로 분리하는 작업이다. 현재 상태를 완성된 GraphQL 서버나 전체 테스트 통과로 표현하지 않는다.
