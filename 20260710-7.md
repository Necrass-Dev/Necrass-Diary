# 2026-07-10 개발일지 7

## 오늘의 목표

async-graphql에서 root query field container와 일반 GraphQL object가 동일한 `#[Object]` macro를 사용하는 구조를 관찰하고, Pothos의 schema builder 표면과 비교한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx/src/db/schema.rs`에 `OrganizationRow`를 추가했다.
  - id, name, slug, created_at 컬럼을 SQLx `FromRow` 타입으로 정의했다.
- `src/graphql/organization.rs`에 Organization과 OrganizationQuery의 골격을 추가하기 시작했다.
- `src/graphql/mod.rs`에 organization module 선언을 추가했다.

## 구현 메모

현재 viewer 구현에는 두 종류의 GraphQL object container가 있다.

```rust
#[Object]
impl User {
    // id, username, displayName, avatarUrl
}

#[Object]
impl ViewerQuery {
    // viewer
}
```

`User`는 GraphQL 응답에서 선택되는 일반 object type이고, `ViewerQuery`는 `MergedObject`로 최종 Query root에 합쳐지는 root field container다. 두 타입의 역할은 다르지만 async-graphql에서는 같은 `#[Object]` macro와 method 형태로 선언한다.

## 실험 및 관찰

Pothos에서는 역할이 코드 표면에서 더 직접적으로 구분된다. `builder.queryField("viewer", ...)`는 root query field를 추가한다는 사실을 호출 자체로 보여 주고, `builder.drizzleNode("users", ...)`와 `t.exposeString(...)`은 User object의 field를 선언한다는 것을 보여 준다.

async-graphql에서는 `#[Object]`가 "GraphQL field를 가진 object"라는 공통 추상화만 드러낸다. 이것이 Query root인지, 일반 output object인지, MergedObject에 넣기 위한 부분 root인지 판단하려면 Rust type 이름, schema builder에 전달되는 위치, MergedObject tuple 구성을 함께 읽어야 한다.

특히 Pothos처럼 도메인 파일에 root field를 함께 두기 위해 `ViewerQuery`를 만든 경우, User object와 별도로 또 하나의 `#[Object]` impl이 생긴다. `#[Object]`를 검색해도 object field와 root field가 섞여 나오며, GraphQL schema의 구조가 macro attribute가 아닌 주변 Rust 조립 코드에 분산된다.

이는 `#[Object]`가 잘못된 API라는 뜻은 아니다. root query도 GraphQL object라는 타입 시스템상의 공통점은 맞다. 다만 framework 사용자 입장에서는 API surface의 역할을 즉시 식별할 수 있는 표지가 줄어든다. Pothos의 명시적 builder method와 비교했을 때, 코드 탐색과 리뷰에서 더 많은 문맥 복원이 필요하다.

`cargo check`는 통과했다. OrganizationRow와 Organization 관련 골격은 아직 resolver와 Query root 병합에 연결되지 않아 dead-code 경고가 발생한다.

## 결정 사항

- async-graphql 실험에서는 도메인별 root field container가 일반 output object와 구분되도록 `ViewerQuery`, `OrganizationQuery`처럼 역할을 type name에 명시한다.
- `MergedObject`를 사용한 root 조립 과정과 `#[Object]`의 역할 혼동은 별도 관찰 항목으로 유지한다.
- convenience abstraction으로 이 차이를 감추기 전에 Organization, Project까지 같은 구조를 구현해 실제 반복 비용을 확인한다.

## 다음 작업

- Organization GraphQL object의 field resolver와 OrganizationQuery.organization(slug:)를 완성한다.
- OrganizationQuery를 최종 Query MergedObject에 포함한다.
- viewer와 organization root field를 나란히 두고, Pothos의 queryField 구조와 코드 탐색 비용을 비교한다.
