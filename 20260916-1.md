# 2026-09-16 개발일지 1

## 오늘의 목표

SDL 변경을 감지하는 개발 도구의 역할과 SDL·Rust 구현 사이의 동기화 방향을 결정한다. [9월 14일의 Cargo 기반 워크플로우](20260914-1.md)를 이어서 구체화한다.

## 변경 사항

이번 작업은 설계 합의의 기록이며, watcher나 코드 생성기를 구현하지 않았다. `necrassrs`에는 작업 트리 변경이 없고, `necrassrs-lab`의 기존 미커밋 변경은 이번 기록 작업에서 수정하지 않았다.

## 논의 및 관찰

SDL에서 쿼리가 삭제되거나 인자·반환 타입이 변경되면 기존 Rust 구현과 생성된 계약이 맞지 않을 수 있다. 이를 해결하기 위해 사용자 Rust AST와 SDL을 양방향으로 자동 수정할지, SDL을 기준으로 생성하고 구현의 불일치를 검출할지 논의했다.

Rust 함수의 삭제만으로 API 폐기, 리팩터링, 실수를 구분할 수 없다. 또한 SDL에서 삭제된 쿼리의 구현에도 재사용할 업무 로직이 남아 있을 수 있다. 따라서 자동화의 범위는 생성 코드 갱신과 계약 불일치 검출로 정하고, 사용자 구현의 수정·삭제는 개발자가 판단한다.

## 결정 사항

### 공개 API는 SDL First로 유지한다

- `*.graphql`을 공개 API 계약의 기준으로 삼는다.
- SDL에서 Rust 타입, resolver trait, 필드 연결 및 등록 코드를 생성한다.
- Rust 구현의 변경을 SDL에 자동 반영하지 않는다.
- 생성 코드는 `OUT_DIR`, 사용자 구현은 `src/`에 분리한다. 재생성 과정에서 사용자 구현을 덮어쓰거나 삭제하지 않는다.
- 생성된 계약과 Rust 구현의 불일치는 컴파일러로 검출한다. 양쪽 AST를 분석하여 서로 수정하는 별도 동기화 엔진은 만들지 않는다.

### 변경별 처리

| 변경 | 생성 및 검증 동작 |
| --- | --- |
| SDL에 사용자 구현이 필요한 쿼리 추가 | 필수 resolver trait 메서드를 생성하고 구현 누락을 컴파일 오류로 검출한다. |
| SDL에서 쿼리 삭제 | 해당 메서드와 스키마 등록을 생성 코드에서 제거한다. 기존 trait 구현에 남은 메서드는 컴파일 오류로 드러난다. |
| SDL의 인자·반환 타입 변경 | Rust 시그니처를 재생성하고 기존 구현과의 타입 불일치를 검사한다. |
| Rust에서 필수 resolver 구현 삭제 | 컴파일 오류로 알리며 SDL 계약은 유지한다. |
| Rust에 일반 함수 추가 | 공개 API에는 반영하지 않는다. |

SDL에서 제거한 쿼리의 구현은 사용자가 삭제하거나 일반 업무 함수로 옮긴다. 별도 일반 함수로 남은 코드까지 반드시 컴파일 오류가 발생하는 것은 아니다. 다만 SDL에서 제거된 쿼리는 갱신된 스키마에 노출되지 않는다. 단순 필드의 자동 연결 원칙은 유지하며, 모든 필드를 수동 resolver 구현 대상으로 만들지는 않는다.

### 변경 감지 명령은 `necrass watch`로 정한다

개발 중 SDL 변경을 감지하고 생성 코드 갱신 및 검증 결과를 보여주는 명령으로 `necrass watch`를 사용한다. 논리적인 처리 순서는 다음과 같다.

```text
SDL 변경 감지 → SDL 검증 및 Rust 코드 재생성 → Rust 구현 검사 → 오류 표시
```

기존 Cargo 워크플로우와 같은 생성 경로를 사용한다. `cargo check`가 `build.rs`의 생성 단계를 실행하도록 연결하면 watcher가 코드 생성을 별도로 중복 수행할 필요는 없다. 구체적인 파일 감시 구현은 후속 작업에서 정한다.

`necrass watch`는 선택적인 개발 편의 명령이다. 실행하지 않아도 `cargo build`만으로 SDL 코드 생성과 컴파일이 수행되어야 한다. 별도 `necrass compile`을 필수 선행 명령으로 두지 않는 기존 결정도 유지한다. 서버 자동 재시작은 이번 합의의 범위에 포함하지 않는다.

## 검증

- 변경 수집 스크립트와 저장소 상태를 확인하고 기존 일지와 설계의 일관성을 검토했다.
- 이번 기록은 설계 결정이며 코드 생성, 컴파일 오류, 파일 감시의 실제 동작을 검증한 결과는 아니다.
- 구현 코드를 변경하지 않았으므로 코드 테스트와 서버 실행은 수행하지 않았다.

## 다음 작업

1. 최소 프로토타입에서 SDL 변경 시 `build.rs`가 다시 실행되는지 확인한다.
2. 쿼리 추가·삭제와 시그니처 변경이 의도한 컴파일 오류로 드러나는지 확인한다.
3. 생성 코드 갱신 시 사용자 구현 파일이 보존되는지 검증한다.
4. 같은 Cargo 생성 경로를 호출하는 `necrass watch`의 최소 구현을 검토한다.
