# 2026-07-26 개발일지 1

## 오늘의 목표

async-graphql/SQLx 실험 서버에서 `Project.issues` Relay connection의 입력 타입과 기본 조회 구조를 작성하고, SQL 결과를 GraphQL edge로 변환하는 책임을 검토한다.

## 변경 사항

- `services/api-async-graphql-sqlx/src/graphql/issue.rs`
  - `IssueStatus`, `IssueOrderField`, `OrderDirection` GraphQL enum을 추가했다.
  - `IssueFilter`, `IssueOrder` GraphQL input object를 추가했다.
- `services/api-async-graphql-sqlx/src/graphql/project.rs`
  - `Project.issues(after, first, filter, orderBy)` field의 골격을 추가했다.
  - `issues` 테이블을 `project_id`, `updated_at DESC`, `id DESC` 기준으로 조회하도록 작성했다.
  - `OpaqueCursor<(updated_at, id)>`를 사용해 issue row를 `Edge`로 변환하는 코드를 추가했다.

## 구현 메모

`async_graphql::connection::query`는 GraphQL 문자열 커서를 `OpaqueCursor` 타입으로 디코드하고, 음수 페이지 크기를 검증한다. resolver는 SQLx가 반환한 `Vec<IssueRow>`를 다음 두 단계로 처리한다.

1. `first + 1`개를 조회하고, 추가 한 건의 존재 여부로 `has_next_page`를 계산한다.
2. 현재 페이지의 row마다 `(updated_at, id)` 커서와 `Issue::from(row)`를 만들어 `Edge`로 변환한다.

`updated_at`만으로는 같은 시각에 갱신된 issue의 순서를 보장할 수 없으므로, `id`를 보조 정렬 및 커서 값에 포함했다.

## 실험 및 관찰

Rust iterator로 `Vec<IssueRow>`에서 `Vec<Edge<OpaqueCursor<(String, String)>, Issue>>`를 직접 만들 수 있다.

```rust
rows.into_iter()
    .map(|row| Edge::new(OpaqueCursor((row.updated_at.clone(), row.id.clone())), Issue::from(row)))
    .collect()
```

따라서 row-to-edge 변환 자체는 별도 루프나 intermediate collection을 요구하지 않는다. 그러나 Relay 응답은 `edges` 외에 `pageInfo`를 제공해야 하므로 `Connection` 객체는 필요하다. 현재 async-graphql API는 빈 `Connection`을 만든 뒤 public `edges` 필드에 collection을 대입하는 형태다.

Pothos의 `relatedConnection`과 비교하면, async-graphql은 relation metadata, 정렬, 필터, cursor predicate, `hasNextPage` 계산을 연결하지 않는다. resolver가 DB 조회와 Relay 포장을 모두 담당한다.

## 결정 사항

- 실험 서버에서는 우선 현재의 직접 iterator 변환을 유지한다. 이 부분은 한 줄의 표준 iterator 처리이며 별도 helper를 만들 이유가 없다.
- NecrassRs에서 connection 편의 API를 검토할 때는 단순 `Vec<T> -> Edge<T>` helper가 아니라, cursor 선택 함수와 페이지 정보까지 함께 받는 좁은 API여야 한다.
- SQL cursor predicate, filter, order direction은 아직 구현하지 않았다. `after`가 주어졌을 때 `(updated_at, id)` 키셋 조건을 SQL에 추가해야 실제 다음 페이지가 첫 페이지를 반복하지 않는다.

## 다음 작업

- `after` 커서를 이용한 `updated_at`/`id` 키셋 pagination 조건을 SQLx query에 추가한다.
- `IssueFilter`의 status, assigneeId, search 조건을 추가한다.
- `IssueOrder.direction`에 따라 정렬 및 cursor 비교 방향을 전환한다.
- connection query를 실행하는 GraphQL 테스트를 추가한다.
