# 2026-07-10 개발일지 1

## 오늘의 목표

async-graphql과 sqlx 조합으로 Rust GraphQL 서버 실험을 시작하기 위한 초기 발판을 만들고, 앞서 진행한 Pothos/Drizzle 실험과 비교할 관찰 지점을 정리했다. 특히 async-graphql의 첫인상은 "러스트가 아니라 매크로로 작성하는 느낌"에 가깝다는 점을 기록한다.

## 변경 사항

- `necrassrs-lab/services/api-pothos-drizzle`에서는 1차 MVP에 필요한 GraphQL API가 상당 부분 채워졌다.
  - `viewer`, `organization(slug:)`, `project(key:)` 같은 root query를 구현했다.
  - `Project.issues`는 `IssueFilter`, `IssueOrder`, Relay connection, Drizzle relation query를 조합해 pagination/filter/order 실험 지점으로 만들었다.
  - `createIssue`, `updateIssue`, `changeIssueStatus`, `addComment`, `changeMemberRole` mutation을 추가했다.
  - mutation payload는 `{ entity, errors }` 형태로 통일했고, `UserError`에 `code`, `message`, `path`를 둬 domain error를 GraphQL 응답 안에 담는 패턴을 확인했다.
- `necrassrs-lab/plan.md`에는 실험 API의 우선순위와 검증할 페인 포인트를 구체화했다.
  - 1차 MVP의 핵심 쿼리는 `project(key).issues`로 잡았다.
  - `updateIssue`는 omitted/null/value 차이를 반드시 관찰할 mutation으로 지정했다.
  - union/interface 성격의 `IssueActivity` 계열은 3차 이후로 미뤄, Pothos 학습 곡선과 실험 범위가 섞이지 않게 했다.
- `necrassrs-lab/services/api-async-graphql-sqlx`를 새 Rust 실험 대상으로 열었다.
  - `Cargo.toml`에 `tokio`, `axum`, `async-graphql`, `async-graphql-axum`, `sqlx`를 추가했다.
  - `async-graphql`에는 `dataloader` feature를 켰다.
  - `sqlx`에는 `runtime-tokio`, `sqlite`, `macros`, `migrate` feature를 켰다.
  - Relay ID, generated ID, timestamp, diagnostics, snapshot test 용도로 `base64`, `uuid`, `time`, `anyhow`, `tracing`, `tracing-subscriber`, `serde_json`, `insta`, `tower`를 추가했다.
  - `src/main.rs`는 아직 `Hello, world!` 상태라, 실제 GraphQL schema와 resolver 구현은 다음 단계로 남아 있다.

## 구현 메모

Pothos 구현에서 가장 중요한 관찰은 GraphQL 계약과 DB 접근이 builder DSL 안으로 강하게 들어간다는 점이었다. `builder.drizzleNode`, `t.relation`, `t.relatedConnection`, `t.globalID`를 쓰면 Relay node, global ID, relation field, connection field를 빠르게 만들 수 있다. 대신 API의 실제 모양은 TypeScript 타입과 builder 호출이 섞인 코드 조각을 따라가야 보인다.

`updateIssue`는 omitted/null/value 구분을 확인하기 좋은 사례가 됐다. `UpdateIssueInput`의 `title`, `description`, `assigneeId`는 optional field로 선언했고, resolver에서는 `"title" in input`, `"description" in input`, `"assigneeId" in input`으로 필드가 전달됐는지를 먼저 본다. 그 뒤 `null`은 값 제거로, property 부재는 변경 없음으로 처리한다. 이 동작은 Necrass에서 입력값 의미론을 `Option<T>` 하나로 접으면 안 된다는 기존 판단을 다시 확인시킨다.

async-graphql 쪽은 아직 의존성만 잡은 상태지만, 이미 방향성이 보인다. Rust 코드에 자연스러운 trait/impl을 쓰기보다 `#[Object]`, `#[derive(SimpleObject)]`, `#[derive(InputObject)]`, `#[derive(Enum)]`, field attribute 같은 proc-macro 표면을 중심으로 schema를 작성하게 될 가능성이 크다. 그래서 첫인상은 Rust 애플리케이션 코드를 작성한다기보다, Rust 문법 안에서 async-graphql 매크로 DSL을 작성하는 느낌에 가깝다.

이 느낌은 단순 취향 문제가 아니다. Necrass가 피하려는 지점은 "GraphQL 계약이 보이지 않는 매크로 확장 결과로 숨어 들어가는 것"이다. 작은 예제에서는 매크로가 boilerplate를 줄여주지만, resolver 수와 input/output 타입이 늘어나면 rust-analyzer, compile error, schema diff, review 단위가 모두 매크로가 만든 경계에 영향을 받는다.

## 실험 및 관찰

Pothos/Drizzle 쪽은 schema builder가 프레임워크 DSL이라는 점을 숨기지 않는다. `builder`가 API 표면의 중심이고, Drizzle plugin과 Relay plugin을 등록한 뒤 그 세계 안에서 타입을 선언한다. GraphQL을 TypeScript로 직접 작성한다기보다 Pothos가 제공하는 타입 안전 builder를 조립하는 느낌이다.

async-graphql도 비슷한 위험이 있지만, Rust에서는 proc-macro가 개입하기 때문에 체감이 더 무겁게 다가올 수 있다. Rust의 장점은 명시적인 타입, trait, 모듈 경계인데, GraphQL 타입 선언이 derive/attribute로 흩어지면 실제 계약은 소스 코드와 매크로 확장 사이에 걸린다. "러스트가 아니라 매크로로 작성하는 느낌"이라는 첫인상은 이 지점을 가리킨다.

현재 `api-async-graphql-sqlx`는 아직 실행 가능한 GraphQL 서버가 아니므로, async-graphql에 대한 평가는 초기 인상 수준이다. 다만 `Cargo.toml`에서 `async-graphql`의 `dataloader` feature와 `sqlx`의 `macros` feature를 동시에 켠 상태는 앞으로 관찰할 포인트를 분명히 한다. GraphQL schema 매크로, SQL query/check 매크로, async runtime, loader 패턴이 한 프로젝트 안에서 어떤 개발 경험을 만드는지 확인해야 한다.

## 결정 사항

- Necrass의 핵심 비교 기준은 "기능을 얼마나 빨리 붙일 수 있는가"보다 "계약이 어디에 존재하고 어떻게 검증되는가"로 둔다.
- async-graphql 실험에서는 처음부터 큰 구조를 만들지 않고, Pothos에서 이미 구현한 MVP API를 같은 순서로 옮겨 비교한다.
- `updateIssue`의 omitted/null/value 구분은 반드시 async-graphql에서도 재현한다. 이 결과는 Necrass의 input coercion과 resolver argument 타입 설계에 직접 반영한다.
- async-graphql의 dataloader는 기능 사용 여부만 보지 않고, request scope loader를 어디에 두어야 하는지와 resolver 작성자가 얼마나 의식해야 하는지를 함께 기록한다.
- Necrass는 매크로 중심 code-first API를 그대로 따라가기보다, contract-first codegen 또는 구조화된 IR을 먼저 검토하는 방향을 유지한다.

## 다음 작업

- `api-async-graphql-sqlx`에 axum 서버, async-graphql schema, request context, sqlite connection pool을 붙인다.
- Pothos 실험과 같은 순서로 `viewer`, `organization(slug:)`, `project(key:)`, `project.issues`를 구현한다.
- Relay global ID 표현을 async-graphql에서 어떻게 모델링할지 확인한다.
- `updateIssue`를 구현하면서 omitted/null/value가 어떤 Rust 타입과 매크로 조합으로 표현되는지 기록한다.
- schema 출력 또는 snapshot test를 추가해, 코드 변경이 GraphQL 계약에 어떻게 드러나는지 확인한다.
