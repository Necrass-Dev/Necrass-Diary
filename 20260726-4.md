# 2026-07-26 개발일지 4

## 오늘의 목표

`node(id:)` 구현에서 global ID type discriminator를 실제 DB row 조회로 전환하는 비용을 정리한다.

## 변경 사항

- `services/api-async-graphql-sqlx/src/graphql/node.rs`에 `Node` interface와 `NodeQuery` 골격이 추가된 상태다.
- `User`, `Project`, `Issue`의 `id` resolver visibility가 `pub(crate)`로 변경되어 `Interface` derive가 sibling module에서 공통 `id` field를 호출할 수 있게 됐다.
- `Project::from`이 node resolver에서 재사용 가능하도록 공개됐다.
- `NodeQuery`가 root `Query`의 `MergedObject`에 추가됐다.

## 구현 메모

global ID는 decode 후 `NodeKind`와 raw DB ID로 분리된다. 최종 resolver는 `NodeKind::User`, `NodeKind::Project`, `NodeKind::Issue` branch마다 다음을 반복해야 한다.

1. 해당 row type을 선택한다.
2. 해당 테이블과 SELECT column 목록을 작성한다.
3. raw DB ID를 bind한다.
4. `fetch_optional` 결과를 object conversion으로 바꾼다.
5. `Node` enum variant로 감싼다.

## 실험 및 관찰

connection 구현과 달리 Node는 cursor, filter, 정렬, page info 조합이 없어서 global ID를 type별 조회로 전환하는 제어 흐름은 단순하다.

그러나 실제 SQLx 구현은 type별 row type, SQL SELECT, bind, 오류 변환, row-to-object conversion을 각각 반복한다. `NodeKind`가 추가될 때마다 branch가 한 개씩 증가하므로, 전환 구조가 쉬운 것과 구현이 적은 것은 다르다.

Pothos의 node 설정은 schema metadata와 Drizzle table 정의에서 이 반복을 흡수한다. 현재 async-graphql/SQLx 구성에서는 resolver가 이를 직접 작성한다.

## 결정 사항

- 현재 세 종류의 node만 지원하므로 별도 generic repository 또는 factory는 만들지 않는다.
- `NodeQuery`에서 세 branch를 명시적으로 작성해 반복 비용을 먼저 관찰한다.
- malformed global ID는 error, 유효하지만 조회 결과가 없는 ID는 null이라는 동작을 유지한다.

## 다음 작업

- `NodeQuery.node`에 `decode`와 `NodeKind`별 SQLx `fetch_optional`을 구현한다.
- 반환값을 `Node::User`, `Node::Project`, `Node::Issue`로 변환한다.
- global ID를 사용한 `node(id:)` GraphQL 테스트를 추가한다.
