# 2026-07-26 개발일지 5

## 오늘의 목표

`Comment`를 Node로 확장하며 반복되는 SQLx/async-graphql 코드를 관찰하고, 재사용성 부족을 통제할 방향을 정리한다.

## 변경 사항

- `services/api-async-graphql-sqlx/src/db/schema.rs`
  - `CommentRow`를 추가했다.
- `services/api-async-graphql-sqlx/src/graphql/comment.rs`
  - `Comment` object와 row-to-object conversion을 추가했다.
- `services/api-async-graphql-sqlx/src/graphql/global_id.rs`
  - `NodeKind::Comment`의 encode/decode 지원을 추가했다.
- `services/api-async-graphql-sqlx/src/graphql/node.rs`
  - `Node::Comment` variant와 comments table 조회 branch를 추가했다.
- `services/api-async-graphql-sqlx/src/graphql/mod.rs`
  - comment module을 등록했다.
- `cargo check`가 경고와 함께 성공했다.

## 구현 메모

Comment 하나를 추가하기 위해 다음 변경이 필요했다.

1. SQLx `FromRow` struct를 추가한다.
2. GraphQL object와 row conversion을 추가한다.
3. global ID type discriminator의 enum, encode, decode를 확장한다.
4. Node interface variant를 추가한다.
5. `node(id:)` resolver에 SELECT, bind, fetch, 오류 변환, enum wrapping branch를 추가한다.

이 흐름은 User, Project, Issue에서도 같은 형태로 반복된다.

## 실험 및 관찰

Node type 전환의 제어 흐름은 단순하지만, 타입 하나를 늘릴 때마다 여러 파일에 동일한 종류의 정보를 반복 선언한다. table 이름, DB ID column, row type, GraphQL type, scalar field, global ID kind, Node variant가 분산되어 있다.

단순 generic repository로 이 문제를 감추기는 어렵다. `Node`의 반환값은 이질적인 GraphQL object enum이고, relation, authorization, pagination은 type별 동작이 다르다. 런타임 generic은 SQL 문자열과 type conversion 문제를 없애지 못한 채 오류 위치만 멀어지게 할 가능성이 높다.

반복 중 상당수는 규칙적이다. 단순 column field, row conversion, global ID encode/decode, Node interface branch는 하나의 선언에서 생성할 수 있다. 반대로 relation resolver, filter/order query, connection cursor, authorization, custom mutation 검증은 생성 대상이 아니라 명시적인 hook으로 남겨야 한다.

## 결정 사항

- 현 실험 코드에 범용 repository, factory, trait hierarchy를 추가하지 않는다.
- NecrassRs의 후보 방향은 SDL만이 아니라 DB mapping 정보를 포함한 schema IR 또는 선언형 Rust macro다.
- 이 IR은 단순 object/node boilerplate를 생성하고, relation과 connection은 user-authored resolver hook으로 위임해야 한다.
- 다음 별도 실험에서는 User 또는 Comment 하나만 대상으로, 선언 하나에서 row mapping과 Node branch를 생성하는 최소 codegen 형태를 검증한다.

## 다음 작업

- `Issue.comments(first, after)` connection을 구현해 relation pagination의 반복 비용을 추가로 관찰한다.
- `Comment.author` relation을 구현해 두 번째 N+1 지점을 확인한다.
- codegen 후보를 별도 실험으로 분리하고, 생성 범위와 custom hook 경계를 비교한다.
