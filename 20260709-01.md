# Necrass를 위한 Rust GraphQL 구현체 심층 조사 보고서

이 보고서는 0708에, 실제 네크라스 제작의 발단이 된 대화록과 그 외 조사기록을 요약한 것입니다.

## 핵심 판단

조사해보면, 현재 Rust의 GraphQL 서버 구현체들이 겪는 가장 큰 문제는 “기능 부족” 하나로 요약되지 않습니다. 더 본질적인 문제는 GraphQL의 계약 지향적 성격과 Rust의 정적 타입·트레이트·매크로 중심 설계가 자주 충돌한다는 점입니다. 그 결과 실제 현장에서는 nullability 표현, `null`과 “아예 안 보냄”의 구분, interface/union의 모델링, 동적 스키마, 스키마-우선 개발 흐름, 프로시저 매크로 기반 IDE/컴파일 경험, N+1 해결 방식이 반복적으로 병목이 됩니다. GraphQL 스펙 자체도 wrapper type과 입력 규칙이 계속 확장되고 있고, 2025년 판에서는 `@oneOf`, 입력값 deprecation, schema coordinates 같은 항목이 추가되어 구현체가 계속 따라가야 하는 부담도 커졌습니다.

사용자께서 제공한 디스코드 대화록의 문제의식도 상당 부분 실제 코드와 이슈 트래커에 맞닿아 있습니다. 다만 “async-graphql이 내부적으로 nullability를 `!` suffix 문자열로만 구분한다”는 말은 절반만 맞습니다. `async-graphql`의 동적 타입 API에는 `TypeRef::Named`, `TypeRef::NonNull`, `TypeRef::List`라는 구조화된 enum이 존재하지만, 동시에 registry 메타데이터의 `MetaInputValue.ty`와 `MetaField.ty`는 `String`으로 저장되고, interface 정의용 매크로에서도 `ty = "String"` 같은 문자열 기반 표기가 여전히 노출됩니다. 즉, 일부 층위에서는 구조화된 IR을 쓰지만 다른 층위에서는 다시 SDL 유사 문자열로 내려앉는 이중 구조가 존재합니다. 이것이 바로 Necrass가 피해야 할 “반쯤 타입 안전하고 반쯤 stringly-typed한” 설계 냄새입니다.

## Rust와 GraphQL이 충돌하는 지점

GraphQL 스펙은 타입이 기본적으로 nullable이라고 가정하고, `Non-Null`과 `List`를 wrapper로 조합합니다. 공식 문서와 스펙은 모두 “기본은 nullable”이며 `!`는 wrapper라는 점을 분명히 합니다. 반면 Rust는 일반적으로 `T`가 값 존재를 전제하고, nullable을 `Option<T>`로 별도 감쌉니다. 이 차이는 단순 문법 차이가 아니라, 구현체 내부에서 타입을 어떻게 표현해야 하는지에 직접 영향을 줍니다. 문자열 `"String!"`처럼 SDL 표기를 그대로 보관하면 빠르게 구현할 수 있지만, wrapper 조합에 대한 구조적 검사나 subtype 판정, list/member nullability 검증은 훨씬 취약해집니다. 반대로 `Named/List/NonNull` 트리로 보관하면 검증과 변환은 안정적이지만 API가 더 설계 비용을 요구합니다. 

두 번째 충돌은 입력 의미론입니다. GraphQL에서는 `arg: null`과 `arg`를 아예 생략하는 것이 비슷하지만 동일하지 않습니다. 그래서 업데이트 mutation 같은 곳에서는 “값을 null로 지운다”와 “손대지 않는다”를 구분해야 합니다. `async-graphql`은 이 문제를 위해 `MaybeUndefined<T>`를 제공하고, Juniper는 `Nullable<T>`를 따로 둡니다. 즉, Rust 구현체가 순수한 `Option<T>`만으로는 충분하지 않다는 사실을 스스로 인정하고 별도 삼중 상태 타입을 도입한 셈입니다. Necrass가 이 부분을 코어 개념으로 승격하지 않으면, 결국 사용자 API 어딘가에 임시 enum이나 이중 `Option<Option<T>>`가 새어 나올 가능성이 큽니다.

세 번째는 interface/union입니다. Juniper 문서는 GraphQL interface의 가장 가까운 Rust 대응물이 trait라고 설명하면서도, GraphQL interface는 “추상화이면서 동시에 값”인 반면 Rust trait는 추상화일 뿐 값 타입이 아니라고 명시합니다. 그래서 실제 값 표현에는 trait object나 implementer enum 같은 별도 박스가 필요합니다. 이것은 단순한 설명이 아니라 Rust GraphQL 구현체의 설계 난제를 그대로 드러냅니다. interface/union이 많아질수록 “실제 데이터 타입”, “그래프상 추상 타입”, “다운캐스트용 wrapper”가 분리되고, API가 복잡해집니다.

여기에 더해 GraphQL에는 generics 개념이 없기 때문에 Rust 쪽 generic 추상화를 그대로 노출하기 어렵습니다. Juniper 유지보수자는 `GraphQLInputObject` 매크로가 generics와 잘 맞지 않는다는 질문에 대해, 문제의 핵심이 “GraphQL 타입 시스템에 generics가 없다”는 데 있다고 답했습니다. 즉, 많은 불편은 라이브러리 품질 문제보다 “언어 모델이 다르다”는 데서 옵니다. 그러나 바로 그렇기 때문에 Necrass는 이 차이를 숨기기보다, 외부 계약과 내부 Rust 타입을 의도적으로 분리하는 쪽이 더 유리합니다.

## async-graphql에서 드러난 구조적 문제

`async-graphql`은 현재 Rust 쪽에서 가장 기능이 풍부한 축에 속합니다. 공식 문서는 이 라이브러리가 스펙 호환성, async/await, dynamic schema, Apollo Federation v2, batch query, subscriptions, file upload 등을 지원한다고 설명합니다. 문제는 “기능이 많다”는 사실이 곧 “설계가 단순하다”를 뜻하지는 않는다는 점입니다. 오히려 기능 표면적이 넓은 만큼, contract source of truth가 어디인지와 타입 표현이 어느 층위에서 구조화되어 있는지가 더 중요해집니다.

async-graphql의 가장 흥미로운 지점은 타입 표현이 계층마다 일관되지 않다는 것입니다. 동적 스키마 API의 `TypeRef`는 `Named/NonNull/List` enum으로 잘 설계되어 있고, `Display` 구현으로 SDL 문자열을 직렬화합니다. 그런데 registry 레벨에서는 `MetaInputValue.ty`와 `MetaField.ty`가 `String`이고, interface 정의 매크로 예제도 `field(name = "area", ty = "f32")`처럼 문자열 기반 타입 표기를 요구합니다. 다시 말해, 코어 한쪽에서는 wrapper tree를 알고 있지만 다른 한쪽에서는 다시 문자열 계약으로 떨어집니다. 이런 혼합 구조는 subtype 판정, 등록 순서, 사용성, 오류 메시지 품질에서 장기적으로 비용이 큽니다. 디스코드 대화록의 문제의식이 async-graphql을 겨냥했다면, 바로 이 지점을 찌른 것으로 보는 편이 정확합니다.

또 하나의 문제는 code-first가 너무 강하다는 점입니다. schema-first 지원은 2020년에 별도 parser/codegen 아이디어로 제안됐지만, 2026년 커뮤니티 답변에서는 여전히 “async-graphql은 code-first가 설계 원칙이며, juniper-from-schema에 해당하는 직접적 대안은 없다”고 설명합니다. 그래서 현실적인 운영 패턴이 “실행 가능한 코드 스키마를 SDL로 export해서 CI에서 diff한다”는 식으로 귀결됩니다. 이것은 가능한 workaround이지만, 계약 파일이 소스가 아니라 산출물이라는 뜻입니다. 프론트엔드·백엔드가 공통 SDL을 중심으로 협업하는 조직에서는 이 흐름이 곧 drift와 리뷰 피로를 낳습니다.
프로시저 매크로 중심 UX도 무시하기 어렵습니다. 실제 사용자 보고에 따르면 schema가 약 1700줄, mutation 약 100개, query 약 35개 수준으로 커졌을 때 async-graphql이 포함된 웹 애플리케이션 빌드가 M1 Pro에서 약 55초, 별도 Linux VM에서는 약 100초까지 걸렸고, rust-analyzer의 문법 검사도 10~12초 지연됐다고 합니다. Seaography가 async-graphql의 dynamic schema 엔진을 활용하면서 “정적 code generation보다 더 빠른 compile time”을 장점으로 내세우는 것도, 이 compile-time 부담이 실제 생태계의 고통이라는 간접 증거입니다. 즉, async-graphql의 기능 집약적 proc-macro UX는 소규모에는 편하지만 대형 스키마에서는 생산성을 갉아먹을 수 있습니다.

동적 스키마 경로는 특히 조심해야 합니다. `async-graphql`은 dynamic schema를 정식 지원하지만, 리스트 안의 nullable object 처리에서 `FieldValue::NULL`이 기대대로 작동하지 않는 버그가 보고되었고, 동적 스키마의 interface subtype 판정에서도 wrapper를 stripping한 뒤 타입 동등성만 보는 듯한 동작 때문에 문제가 제기됐습니다. 수동 등록도 함정입니다. 공식 책은 object가 interface를 구현하더라도 그 interface가 `Schema`에서 직접 참조되지 않으면 registry에 존재하지 않으며, 사용자가 `register_output_type::<MyInterface>()`를 별도로 호출해야 한다고 설명합니다. 이는 “스키마 그래프는 정적으로 보이지만 실제 등록 규칙은 절차적”이라는 뜻입니다. 

마지막으로, N+1 문제도 엔진 차원에서 자동 해결되지는 않습니다. async-graphql은 dataloader 모듈을 “N+1 문제 해결용”이라고 명시하고, 책에서도 별도 장을 할애합니다. 즉, 실행기 자체가 breadth-first batching을 내장했다기보다, 사용자가 요청 범위 loader를 명시적으로 구성해야 합니다. Necrass가 차별화하려면 이 부분을 라이브러리 부가기능이 아니라 execution scheduler의 핵심 기능으로 재설계할 여지가 있습니다. 이 결론은 async-graphql 문서와 이슈를 보면 충분히 끌어낼 수 있는 해석입니다. 

## Juniper에서 드러난 구조적 문제

Juniper는 지금도 중요한 기준점이지만, 성격이 꽤 다릅니다. 공식 문서와 crate 문서는 Juniper가 code-first 접근을 취하며, GraphQL query language는 지원하지만 schema language는 기본적으로 지원하지 않는다고 밝힙니다. schema-first를 원하면 공식 조직 내부 기능이 아니라 외부 crate인 `juniper-from-schema`를 고려하라고 안내합니다. 즉, Juniper는 시작점부터 “계약과 구현을 한 덩어리”로 보는 라이브러리였고, schema-first는 부속 생태계에 위임해 왔습니다. 

Juniper의 가장 눈에 띄는 설계 차이는 nullability 기본값입니다. docs.rs는 Juniper가 “다른 언어의 GraphQL 라이브러리들과 달리” non-null 타입을 기본값으로 만든다고 설명하며, 예를 들어 Rust의 `Vec<Episode>`가 GraphQL에서는 `[Episode!]!`로 변환된다고 적습니다. GraphQL 스펙이 “기본 nullable”인 것과 정반대 방향의 ergonomic bias를 둔 셈입니다. Rust 입장에서는 자연스럽지만, GraphQL 계약만 보는 사람에게는 놀랍고, SDL을 머릿속에서 역산해야 이해되는 API가 되기 쉽습니다. 복합 타입이 많아질수록 `Option<Vec<Option<T>>>` 같은 Rust 타입 체스가 스키마 의도를 가리는 문제가 생깁니다.

interface/union 모델링도 Rust의 제약을 정면으로 드러냅니다. Juniper 문서는 interface가 Rust trait와 다르며, 값 타입을 표현하려면 별도 enum이나 trait object가 필요하다고 설명합니다. `#[graphql_interface]` 문서도 implementer를 전부 열거하는 것이 필수라고 적고, value type 표현을 위해 매크로가 enum을 생성한다고 밝힙니다. 이 방식은 타입 안전성은 높지만, implementer 수가 많거나 플러그인 구조가 필요한 시스템에서는 확장성이 떨어집니다. 실제로 “완전히 동적인 스키마”를 원한다는 요청 이슈는 2022년부터 열려 있고, 제안 내용도 trait 기반 `GraphQLType` API를 우회해 registry를 동적으로 조립할 수 있게 해달라는 것이었습니다. 이는 Juniper가 런타임 동적 스키마에는 본질적으로 불편하다는 뜻입니다.

스펙 추적 비용도 아직 현재진행형입니다. Juniper 문서는 현재 October 2021 스펙 기준 지원을 내세우지만, 2025년 9월 스펙 반영을 위한 공식 roadmap 이슈는 별도로 열려 있습니다. 그 로드맵에는 `@oneOf` input object, 입력값 deprecation, introspection의 `includeDeprecated`, full Unicode, request `extensions` 등 여러 항목이 나열되어 있습니다. 다시 말해 Juniper는 안정적이지만, 최신 GraphQL 표준을 빠르게 흡수하는 엔진이라기보다 점진적으로 따라가는 엔진에 가깝습니다. Necrass가 “새 구현체”를 지향한다면, 이 영역은 단순 기능 비교보다도 “스펙 판별기와 conformance test를 코어에 둘 것인가”라는 질문으로 바꿔 보는 편이 낫습니다.

운영 편의성 측면에서도 기능이 분산되어 있습니다. 구독은 별도 `juniper_subscriptions` crate를 통해 coordinator와 connection 구현을 붙여야 하고, N+1 회피는 dataloader 문서와 `juniper-eager-loading` crate가 별도로 안내됩니다. 심지어 dataloader 문서는 loader를 resolver 내부에서 만들면 batching이 무효화된다고 경고합니다. 이런 구조는 “핵심 엔진은 작은 대신, 실무 기능은 주변 crate와 패턴 지식으로 메운다”는 장점도 있지만, 새 사용자가 시스템의 진짜 best practice를 파악하기 어렵다는 단점도 분명합니다.

매크로 오류 경험도 단정하지 않습니다. 예를 들어 Juniper 이슈에는 함수 변수 이름이 우연히 `args`였다는 이유로 `#[graphql_object]` 확장 이후 엉뚱한 compile error가 터졌다는 사례가 남아 있습니다. 이런 유형의 문제는 proc-macro 기반 프레임워크 전반에서 흔하지만, GraphQL처럼 스키마 표현을 매크로 속성에 많이 실을수록 더 자주 표면화됩니다. 결국 개발자에게 보이는 것은 GraphQL 계약 오류가 아니라 “이상한 Rust 컴파일 오류”가 되기 쉽습니다.

## Tonic 스타일 code generation이 주는 시사점

디스코드 대화록에서 XiNiHa가 Tonic을 예로 든 것은 꽤 설득력이 있습니다. `tonic_prost_build`의 공식 문서는 `build.rs`에서 `.proto`를 컴파일해 Rust 코드를 생성하는 예제를 제시하고, `prost-build`는 service definition을 처리하는 custom code generator를 통해 애플리케이션별 Rust trait를 출력할 수 있다고 설명합니다. 다시 말해 Tonic/Prost 계열은 “계약 파일 → build.rs codegen → 사용자가 generated trait를 구현”이라는 선이 아주 명확합니다.

이 방식의 장점은 단지 코드 생성이 아니라, 계약·생성물·수기 구현의 경계를 분명하게 나눈다는 데 있습니다. GraphQL 쪽 기존 Rust 구현체는 대체로 사용자의 Rust struct/impl에 매크로를 붙여 스키마를 추론하거나, 동적 registry를 프로그래밍적으로 조립합니다. 그 결과 GraphQL 계약이 Rust 구문과 강하게 얽히고, IDE가 보는 정보와 스키마 리뷰 단위가 뒤섞입니다. 반면 Tonic 쪽 모델은 “generated trait가 바로 계약의 Rust 투영본”이기 때문에, 사용자는 business logic만 구현하면 되고, 매크로가 만든 보이지 않는 세계를 덜 추적해도 됩니다. 이 해석은 공식 codegen 문서들로부터 충분히 도출됩니다.

Necrass에 이 관점을 옮기면, source of truth를 GraphQL SDL이나 그보다 더 구조화된 독자 IR로 두고 `build.rs`에서 resolver trait, input/output struct, interface/union wrapper, registration code를 생성하는 모델이 유력합니다. 그렇게 하면 schema-first 조직의 요구를 만족시키면서도, 생성된 Rust 코드는 rust-analyzer와 컴파일러가 직접 볼 수 있습니다. 즉 “코드에 매크로를 붙여 스키마를 숨기기”보다 “스키마로부터 코드를 드러내기”가 됩니다. 이것은 공식 문서의 직접 문구라기보다, Tonic/Prost의 codegen 구조와 Rust GraphQL 라이브러리들의 현재 마찰점을 비교해 얻는 설계적 추론입니다.

## Necrass에 권장되는 설계 원칙

Necrass의 첫 번째 원칙은 **내부 타입 IR의 완전한 구조화**여야 합니다. GraphQL 스펙이 `Named`, `List`, `Non-Null` wrapper 조합을 중심으로 서술하는 만큼, 코어 실행기·validator·registry·introspection은 전부 동일한 wrapper tree를 공유해야 합니다. 문자열 SDL은 입출력 경계에서만 사용하고, 코어 메타데이터에는 들어오지 않게 막는 편이 좋습니다. async-graphql의 `TypeRef`가 보여준 좋은 점은 살리고, registry의 `String` 타입 필드가 보여준 아쉬움은 버리는 방향입니다. 

두 번째 원칙은 **입력값의 `absent / null / value`를 코어 타입으로 승격**하는 것입니다. 이것은 optional helper가 아니라 GraphQL mutation semantics의 핵심입니다. async-graphql의 `MaybeUndefined<T>`와 Juniper의 `Nullable<T>`가 각각 따로 존재한다는 사실 자체가, 이 문제가 라이브러리 가장자리에서 임시로 때울 수 있는 종류가 아니라는 증거입니다. Necrass는 이 상태를 validator, coercion, resolver 인자 타입까지 일관되게 관통시키는 편이 낫습니다. 

세 번째는 **contract-first, codegen-second, handwritten impl-third** 구조입니다. Juniper와 async-graphql 모두 code-first가 강하고, schema-first는 바깥 crate나 CI diff로 보완하는 식입니다. Necrass가 새 엔진이라면 여기에 그대로 합류할 이유가 약합니다. 오히려 SDL 또는 독자 IDL을 canonical source로 두고, generated trait를 tonic처럼 구현하도록 만드는 편이 Rust와 GraphQL의 충돌면을 훨씬 명확하게 관리할 수 있습니다. 

네 번째는 **interface/union의 수동 등록과 수동 열거를 제거하는 자동 생성 모델**입니다. async-graphql은 interface가 schema에서 직접 참조되지 않으면 별도 등록이 필요하고, Juniper는 implementer 열거와 enum wrapper 생성이 사실상 필수입니다. Necrass는 계약 파일에서 interface implementer 집합을 확정하고, generated code가 registration과 runtime downcast wiring을 전부 책임지게 해야 합니다. 그래야 사용자는 interface의 존재론을 추리하지 않고 resolver 구현에만 집중할 수 있습니다. 

다섯 번째는 **N+1을 보조 유틸리티가 아니라 실행기의 정책 문제로 끌어올리는 것**입니다. async-graphql과 Juniper 모두 DataLoader/eager loading을 문서화하고 있지만, 결국 사용자가 요청 범위 로더를 올바른 위치에 두고 배칭을 설계해야 합니다. Necrass는 최소한 field execution scheduler에서 “어떤 단계에서 batch boundary를 만드는가”를 명시적으로 설계해야 하며, 가능하다면 breadth-first 또는 dependency-group 기반 실행을 엔진 옵션으로 가져가는 것이 좋습니다. “기본 엔진은 depth-first, 배칭은 유저 책임”이라는 관성에서 벗어날수록 차별점이 커집니다. 마지막 문장은 문서들을 바탕으로 한 설계적 해석입니다.

여섯 번째는 **스펙 판본별 conformance를 코어 기능으로 두는 것**입니다. Juniper의 2025 스펙 로드맵 이슈가 보여주듯, 최신 GraphQL 스펙을 따라가는 일은 부가 작업이 아니라 제품 경쟁력입니다. 특히 `@oneOf`, 입력값 deprecation, introspection 변화는 codegen과 tooling에도 직접 영향을 줍니다. Necrass는 “어떤 스펙 판본을 목표로 하는지”를 명확히 하고, 판본별 테스트 수트를 내장해 구현 차이를 조기에 드러내야 합니다.

## 최종 평가

정리하면, 기존 Rust GraphQL 구현체의 핵심 문제는 단순히 “불편하다”가 아니라 다음과 같이 구조적입니다. GraphQL의 계약 중심·nullable 기본·partial success·dynamic type system과, Rust의 static type·trait/value 분리·proc-macro UX가 서로 잘 맞지 않습니다. async-graphql은 기능 폭이 넓지만 code-first와 혼합된 타입 표현, 대형 스키마에서의 compile-time 부담, 동적 경로의 복잡성이 문제이고, Juniper는 안정적인 편이지만 nullability 기본값의 반전, 동적 스키마 취약성, 최신 스펙 추적 속도, 외부 crate 의존이 문제입니다. 이 환경에서 Necrass가 승부를 보려면 “또 하나의 derive-macro GraphQL crate”가 아니라, **구조화된 타입 IR + contract-first codegen + first-class input semantics + 엔진 차원의 batching**을 결합한 구현체가 되어야 합니다. 그 방향이야말로 이번 조사에서 드러난 반복 범인을 가장 직접적으로 겨누는 설계입니다.
