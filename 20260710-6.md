# 2026-07-10 개발일지 6

## 오늘의 목표

async-graphql에서 DB user row를 GraphQL `User` object로 노출하는 첫 resolver를 완성하고, Pothos의 `t.exposeString` 기반 field 선언과 비교해 같은 field 수를 선언하면서도 더 불편하게 느껴지는 이유를 관찰한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx/src/graphql/user.rs`에 `User` GraphQL object를 구현했다.
  - `id`, `username`, `displayName`, `avatarUrl` field에 대응하는 Rust method를 각각 `#[Object] impl User`에 추가했다.
  - `UserRow`를 `User`로 변환하는 함수를 추가해 DB row의 `created_at`, `password`, `logged_in`을 GraphQL object에서 제외했다.
- 같은 파일에 `ViewerQuery`를 두고, `x-user-id` request context와 schema data의 `SqlitePool`을 읽어 users 테이블을 조회하도록 구현했다.
  - header가 없으면 `None`을 반환한다.
  - user row가 없으면 `fetch_optional` 결과를 통해 `None`을 반환한다.
- `src/graphql/mod.rs`는 `ViewerQuery`를 `MergedObject`로 합친 `Query` root를 구성한다.
- `src/main.rs`는 Axum `HeaderMap`에서 `x-user-id`를 읽어 `RequestContext`로 주입하고, `Query::default()`를 schema query root로 사용한다.

## 구현 메모

Pothos의 `UserRef`는 `builder.drizzleNode("users", ...)`로 Drizzle table metadata와 GraphQL object field를 연결한다. `username`처럼 DB column을 그대로 노출하는 field는 `t.exposeString("username")`으로 선언한다.

async-graphql에서는 SQLx의 `UserRow`가 GraphQL output type이 아니다. 별도의 `User` struct와 `#[Object] impl User`가 필요하다. 각 field는 Rust method가 되고, method의 반환 타입이 GraphQL field type이 된다.

```rust
#[Object]
impl User {
    async fn username(&self) -> &str {
        &self.username
    }

    async fn avatar_url(&self) -> Option<&str> {
        self.avatar_url.as_deref()
    }
}
```

`UserRow -> User` 변환은 DB 모델의 password와 logged_in이 GraphQL API에 노출되지 않게 막는 경계다. 반면 DB row와 output object의 공통 field를 다시 열거해야 한다. 현재 id는 DB ID를 GraphQL `ID` scalar로 감싼 상태이며, Relay opaque global ID 인코딩은 아직 적용하지 않았다.

## 실험 및 관찰

Pothos도 `t.exposeString`으로 field를 하나씩 선언한다. 따라서 field 선언 수 자체가 async-graphql에서만 새로 생긴 비용은 아니다.

더 불편하게 느껴지는 이유는 한 field의 정보가 여러 Rust 표현으로 분산되기 때문이다. 현재 user 하나에 대해 DB `UserRow` field, GraphQL `User` field, `UserRow -> User` 변환, `#[Object]` resolver method가 따로 존재한다. `t.exposeString`은 Drizzle plugin이 이미 알고 있는 table column을 object builder에 직접 연결하지만, SQLx는 DB metadata와 GraphQL metadata를 연결하지 않으므로 이 경계를 사용자가 모두 작성한다.

또한 단순 getter도 async method, `#[Object]` proc-macro 입력, Rust reference/Option 반환 타입으로 작성해야 한다. Rust 타입에서 GraphQL nullable field로의 변환은 명시적이지만, 객체 field가 많아질수록 실제 resolver 로직보다 framework 형식에 맞추는 코드가 더 눈에 들어온다.

`cargo check`는 통과했다. `UserRow`의 created_at, password, logged_in field는 현재 viewer 변환에서 읽지 않아 dead-code 경고가 발생한다. `cargo run -- --reset-db`는 template2.sqlite를 재생성하고 migration 및 seed 적용을 완료했다.

## 결정 사항

- SQLx DB row와 GraphQL output object는 분리한다. password와 logged_in 같은 내부 컬럼을 output type에 노출하지 않는다.
- viewer root field는 `ViewerQuery`에 두고 `MergedObject`로 `Query`에 합친다.
- 단순 DB column expose가 많은 object에서 async-graphql의 method 반복과 DB-to-output 변환 반복을 계속 관찰한다. 편의 helper를 너무 이르게 만들지 않고, 중복이 실제로 누적되는지 확인한다.

## 다음 작업

- header 유무와 존재하지 않는 user ID에 대한 viewer query를 실행해 null 동작을 확인한다.
- Relay global ID 인코딩/디코딩 helper를 추가하고 User.id에 적용한다.
- `organization(slug:)` query와 Organization object를 구현해 두 번째 도메인에서 같은 반복 구조가 어떻게 늘어나는지 비교한다.
