# 2026-07-26 개발일지 3

## 오늘의 목표

Relay global ID를 기반으로 `node(id:)`의 GraphQL interface와 query root 골격을 추가하고, connection 구현과의 복잡도를 비교한다.

## 변경 사항

- `services/api-async-graphql-sqlx/src/graphql/node.rs`를 추가했다.
  - `User`, `Project`, `Issue` variant를 가진 `Node` interface를 정의했다.
  - `NodeQuery`와 `node(id:)` resolver 골격을 정의했다.
- `services/api-async-graphql-sqlx/src/graphql/mod.rs`
  - `NodeQuery`를 root `Query`의 `MergedObject`에 추가했다.
- `services/api-async-graphql-sqlx/src/graphql/user.rs`
  - `User.id` resolver를 crate 내부에서 interface derive가 호출할 수 있도록 `pub(crate)`로 변경했다.
- `services/api-async-graphql-sqlx/src/graphql/project.rs`
  - `Project::from`과 `Project.id`의 visibility를 node resolver/interface에서 사용할 수 있도록 변경했다.
- `services/api-async-graphql-sqlx/src/graphql/issue.rs`
  - `Issue.id` resolver를 crate 내부에서 interface derive가 호출할 수 있도록 변경했다.

## 구현 메모

async-graphql의 `Interface` derive는 interface에 선언한 `id` field를 각 variant object의 Rust method로 호출한다. object의 `id` method가 sibling module에서 private이면 interface derive가 실패하므로 `pub(crate)` visibility가 필요했다.

`Node` interface의 field type은 GraphQL type name 문자열이 아니라 Rust `ID` 타입을 resolve해야 한다. 따라서 `node.rs`에 `async_graphql::ID` import가 필요하다.

## 실험 및 관찰

connection 구현에서는 relation query, cursor predicate, 정렬 방향, filter, `first + 1`, page info를 하나의 resolver에서 조립해야 했다. 반면 `Node`의 구조는 다음처럼 닫혀 있다.

1. global ID를 decode한다.
2. type discriminator로 user/project/issue 중 하나를 고른다.
3. 해당 테이블에서 raw DB ID로 한 row를 조회한다.
4. interface variant로 반환한다.

이 때문에 interface와 query root를 추가하는 단계는 connection보다 단순하게 느껴졌다. DB 조회 구현은 아직 남아 있지만, pagination처럼 여러 인자의 조합이 SQL 구조를 증식시키지 않는다.

현재 `NodeQuery.node`는 `Ok(None)`을 반환하는 골격이다. `decode`와 `NodeKind` import가 미사용 경고를 내는 상태이며, 아직 실제 node 조회는 구현되지 않았다. `cargo check`는 경고와 함께 성공했다.

## 결정 사항

- `node(id:)`는 별도 `node.rs`에 두고, global ID codec은 기존 `global_id.rs`에 유지한다.
- 첫 구현 범위는 `User`, `Project`, `Issue`로 제한한다. `Organization`과 `Comment`는 이후에 추가한다.
- malformed global ID는 GraphQL error, 유효하지만 존재하지 않는 DB ID는 null로 처리한다.

## 다음 작업

- `NodeQuery.node`에서 `decode(id.as_str())`를 호출한다.
- `NodeKind`별로 `UserRow`, `ProjectRow`, `IssueRow`를 SQLx로 조회하고 Node variant로 변환한다.
- user/project/issue global ID 각각에 대해 `node(id:)`가 원래 type을 반환하는 GraphQL 테스트를 추가한다.
