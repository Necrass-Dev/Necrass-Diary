# 2026-07-22 개발일지 1

## 오늘의 목표

async-graphql의 flat object field 반복을 통해, SDL 또는 구조화된 schema IR에서 field를 선언하고 resolver 코드를 생성하는 접근이 필요한지 검토한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx`에 Issue object 구현이 추가됐다.
  - `IssueRow`는 issues 테이블의 id, project_id, number, title, description, status, priority, assignee_id, reporter_id, created_at, updated_at를 보관한다.
  - GraphQL `Issue`는 공개 field와 이후 relation resolver에 필요한 project_id, assignee_id, reporter_id를 별도로 보관한다.
  - id, number, title, description, status, priority, createdAt, updatedAt을 `#[Object]` method로 각각 노출한다.
- `cargo check`는 통과했다.
  - Issue가 아직 어떤 query에서 반환되지 않아 IssueRow, Issue, 변환 함수가 미사용 경고를 낸다.
  - issue.rs에는 이후 relation 구현을 위해 미리 넣은 Context, Error, Result import가 현재 미사용 경고를 낸다.

## 구현 메모

Issue의 flat GraphQL field는 DB column을 그대로 노출하는 수준이다. 그러나 SQLx row, GraphQL output struct, row-to-output 변환, `#[Object]` getter method에 같은 field 정보가 반복된다.

```rust
async fn title(&self) -> &str {
    &self.title
}

async fn updated_at(&self) -> &str {
    &self.updated_at
}
```

Pothos도 `t.exposeString("title")`처럼 field를 선언한다. 차이는 Drizzle table metadata와 GraphQL field declaration이 같은 builder 호출에 연결되는 반면, async-graphql/SQLx 조합에서는 DB row와 output object 사이의 대응을 Rust 코드 여러 위치에 유지해야 한다는 점이다.

## 실험 및 관찰

SDL에서 field를 선언하고 함수 또는 code generation으로 resolver 표면을 만드는 접근은 유효하다. 단, 단순한 문자열 기반 runtime field factory로 해결하면 Rust 타입 검사와 schema diff 추적을 잃는다.

더 적절한 방향은 SDL 또는 구조화된 schema IR을 GraphQL 계약의 source of truth로 두는 것이다. 예를 들어 object field의 이름, GraphQL type, nullable 여부, DB column 대응을 한 위치에 선언하고, 다음 산출물을 생성한다.

- SDL과 introspection에 나타나는 GraphQL object definition
- Rust resolver trait 또는 function signature
- 단순 column field의 row-to-output mapping
- nullable 및 GraphQL scalar coercion 검증 코드

relation, authorization, computed field는 생성된 resolver 구현을 대체하지 않는다. 이들은 명시적인 custom resolver hook으로 남겨야 한다. 반대로 title, createdAt, updatedAt처럼 DB column을 그대로 노출하는 field는 codegen 대상이므로 application 코드에서 반복할 이유가 없다.

Issue object은 이 분리가 필요한 구체적 사례다. flat field 여덟 개는 정형적이고 생성 가능하지만, assignee, reporter, comments는 relation query, batching, pagination 정책을 요구하므로 생성기만으로 숨기면 안 된다.

## 결정 사항

- Necrass에서는 SDL 또는 구조화된 IR에서 field를 선언하고, Rust resolver와 mapping 코드를 생성하는 contract-first 접근을 계속 검토한다.
- dynamic runtime schema builder를 source of truth로 삼지 않는다. schema name, field type, nullable, resolver 대응은 정적으로 추적 가능해야 한다.
- 단순 column expose와 custom relation resolver를 구분하는 metadata를 설계의 핵심 요구로 둔다.

## 다음 작업

- Project object와 Project.issues query를 구현해 flat field 반복이 두 번째 object에서도 같은 방식으로 나타나는지 확인한다.
- Issue.assignee와 Issue.reporter를 수동 resolver로 추가해 codegen 대상과 custom resolver 대상의 경계를 검증한다.
- SDL/IR에서 단순 column field 하나를 표현하는 최소 문법과 생성 Rust 코드 초안을 작성한다.
