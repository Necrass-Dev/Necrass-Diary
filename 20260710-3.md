# 2026-07-10 개발일지 3

## 오늘의 목표

async-graphql/sqlx 실험 서버에 SQLite 초기화 흐름을 연결하고, GraphQL root type을 분리하기 전 schema를 HTTP 서버 없이 직접 실행하는 테스트 방식을 확인한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx/src/migrations/0001_initial.sql`에 Pothos/Drizzle 실험의 migration 이력을 합친 초기 스키마를 두었다.
  - users, organizations, organization_members, projects, issues, comments 테이블과 기존 인덱스를 포함한다.
  - users에는 로그인 실험에 필요한 password와 logged_in 컬럼이 포함된다.
- `src/db/`에 DB 초기화 관련 모듈을 추가했다.
  - `mod.rs`는 template2.sqlite 경로, SQLite pool 연결, migration, reset 진입점 역할을 맡는다.
  - `schema.rs`와 `seed.rs`는 SQLx row type 및 Pothos와 같은 시드 데이터를 둘 위치로 잡았다.
- `src/main.rs`는 `--reset-db` 인자를 처리하고, 일반 서버 실행에서는 SQLite pool 연결과 migration 적용 후 pool을 async-graphql Schema data로 주입하도록 변경됐다.
- `src/graphql/mod.rs`를 추가해 schema 구성 코드를 옮기기 시작했다.

## 구현 메모

DB 초기화는 서버 기동과 시드 생성을 분리한다. `cargo run -- --reset-db`는 template2.sqlite를 재생성하고 migration과 seed를 적용한다. 일반 서버 기동은 DB pool을 열고 누락된 migration만 적용하며, 시드는 다시 넣지 않는다. 실험 데이터가 매번 중복되지 않고 Pothos 쪽 template.sqlite와 별도 파일을 유지할 수 있다.

async-graphql에서는 schema를 HTTP 요청 없이 직접 실행할 수 있다. 최소 예시는 다음과 같다.

```rust
let schema = Schema::new(Query, EmptyMutation, EmptySubscription);
let response = schema.execute("{ add(a: 10, b: 20) }").await;
```

이 방식은 axum Router, JSON 직렬화, HTTP header를 거치지 않고 resolver 결과와 GraphQL error를 확인한다. Root query나 mutation의 인자 coercion, nullable 동작, payload error 형태를 단위 테스트에 가깝게 검증하기 좋다. HTTP handler test는 request context, header 기반 인증, status/header 처리가 필요할 때만 별도로 추가하면 된다.

## 실험 및 관찰

`cargo check` 결과는 현재 실패한다. 새 `src/graphql/mod.rs`의 최상위에 함수나 item으로 감싸지지 않은 `let schema = ...` 문이 있어 Rust module item으로 해석되지 않는다. schema 생성은 module-level let binding이 아니라 `build_schema` 같은 함수 안에 두거나, 테스트 함수 안에 둬야 한다.

schema 직접 실행은 async-graphql의 장점으로 보인다. GraphQL contract의 핵심 동작을 HTTP 서버와 분리해 빠르게 검증할 수 있기 때문이다. 특히 이후 createIssue, updateIssue, addComment payload의 errors 배열과 input의 omitted/null/value 의미를 Router test보다 작은 단위에서 관찰할 수 있다.

다만 직접 실행 테스트가 database integration을 대체하지는 않는다. SQLx pool, SQLite foreign key, migration, DataLoader batching은 실제 DB를 연결한 별도 test에서 확인해야 한다.

## 결정 사항

- template2.sqlite는 async-graphql/sqlx 실험 전용 DB로 유지한다.
- DB migration의 source of truth는 `src/migrations/0001_initial.sql`이며, Rust row type은 schema DDL을 중복 선언하지 않는다.
- GraphQL root type의 구현 전에는 `Schema::new(...).execute(...)`를 resolver 동작 검증의 기본 방식으로 사용한다.
- `src/graphql/mod.rs`에는 실행문을 직접 두지 않고 schema 생성 함수 또는 test module만 둔다.

## 다음 작업

- `src/graphql/mod.rs`를 컴파일 가능한 schema builder 함수로 정리한다.
- QueryRoot와 MutationRoot를 `graphql/query.rs`, `graphql/mutation.rs`로 분리한다.
- add resolver를 이용해 Schema::execute 기반의 첫 테스트를 작성한다.
- DB reset과 seed가 template2.sqlite에 Pothos와 같은 데이터를 생성하는지 확인한다.
