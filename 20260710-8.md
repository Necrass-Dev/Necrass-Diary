# 2026-07-10 개발일지 8

## 오늘의 목표

issue detail을 준비하면서 async-graphql/SQLx 조합에 Pothos/Drizzle의 `relation()`과 같은 관계 field 조립 기능이 있는지 확인하고, nested relation 구현에 필요한 책임을 정리한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx`에서 organization(slug:) query까지 구현이 완료됐다.
  - Query root는 ViewerQuery와 OrganizationQuery를 MergedObject로 합친다.
  - Organization GraphQL object는 id, name, slug, createdAt field를 method로 정의한다.
- issue detail 작업을 위한 `src/graphql/issue.rs` 파일을 만들었지만, 아직 내용은 없다.
- `cargo check`는 통과했다. UserRow의 일부 내부 컬럼이 아직 viewer 응답 변환에 쓰이지 않아 dead-code 경고가 남아 있다.

## 구현 메모

Pothos/Drizzle 구현에서는 Issue object에 다음과 같이 relation field를 선언할 수 있다.

```ts
assignee: t.relation("assignee"),
reporter: t.relation("reporter"),
comments: t.relatedConnection("comments", ...),
project: t.relation("project"),
```

Drizzle schema에 정의된 foreign key와 relation metadata를 Pothos plugin이 사용하므로, GraphQL object field가 어느 table/column relation을 따라야 하는지 선언적으로 표현된다. relation field의 selection, query shape, connection 조립이 framework와 plugin의 책임 안에 있다.

async-graphql은 GraphQL type system과 resolver 실행만 제공하며, SQLx는 SQL 실행과 row mapping만 제공한다. 두 라이브러리 사이에 table relation metadata를 읽고 GraphQL nested field로 조립하는 계층은 없다.

## 실험 및 관찰

async-graphql에서 Issue.assignee, Issue.reporter, Comment.author 같은 field를 만들려면 다음을 각각 작성해야 한다.

1. parent Issue 또는 Comment struct에 foreign key를 보관한다.
2. `#[Object]` method에서 parent foreign key를 읽는다.
3. context에서 SqlitePool 또는 DataLoader를 꺼낸다.
4. 관계 대상 row를 직접 조회하는 SQL을 작성한다.
5. DB row를 GraphQL output object로 변환한다.
6. list query에서 N+1이 생기지 않도록 DataLoader와 IN query를 별도로 구현하고 schema data에 등록한다.
7. comments처럼 collection relation이면 cursor, pageInfo, edge를 포함한 connection을 직접 조립한다.

이 과정은 foreign key relation 하나를 GraphQL field로 노출하는 데 필요한 정보를 여러 파일과 추상화에 분산한다. MergedObject는 root field를 여러 Rust type으로 나누는 기능일 뿐, ORM relation을 GraphQL resolver로 조립해 주지 않는다.

단일 issue detail에서 assignee와 reporter를 각각 조회하는 것은 기능적으로 구현할 수 있다. 그러나 Project.issues처럼 여러 Issue를 반환하면 relation field 수에 비례해 추가 query가 발생한다. DataLoader는 N+1의 결과를 완화할 수 있지만, relation 자체를 선언하고 batch query를 만들고 등록하는 비용은 남는다.

이는 code-first GraphQL framework 관점에서 중요한 결점이다. Pothos의 relation API는 DB schema 관계를 GraphQL object 관계로 올리는 반복적인 연결 작업을 plugin 수준에서 처리한다. async-graphql/SQLx 조합에서는 같은 기능을 application마다 다시 작성해야 하며, relation field 수가 늘수록 API 구현량과 성능 검증 범위가 함께 증가한다.

## 결정 사항

- async-graphql 실험에서는 Issue.assignee와 Issue.reporter를 먼저 수동 resolver로 구현해 필요한 코드 경계를 확인한다.
- 이후 Project.issues와 Comment.author를 구현할 때 DataLoader를 추가해 N+1 완화에 필요한 추가 구조를 기록한다.
- relation metadata를 GraphQL field로 조립하는 built-in abstraction이 없다는 점을 Necrass 설계 비교의 핵심 평가 항목으로 유지한다.

## 다음 작업

- IssueRow와 Issue GraphQL object를 정의한다.
- Issue.assignee와 Issue.reporter의 수동 nested resolver를 구현한다.
- Relay global ID와 node(id:) lookup을 구현해 issue detail 진입점을 만든다.
- comments connection과 Comment.author를 추가하며 DataLoader 없이 발생하는 query 수를 측정한다.
