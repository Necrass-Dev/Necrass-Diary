# 2026-09-23 개발일지 1

## 오늘의 목표

현재 리졸버 생성 방식에서 스키마 변경 시 사용자 로직이 삭제될 수 있는 문제와 리졸버 구현이 한 파일에 집중되는 문제를 검토하고, XiNiHa님의 대안 설계를 기록한다.

## 변경 사항

이번 항목은 설계 논의 기록이다. `necrassrs`와 `necrassrs-lab`의 코드 및 문서는 변경하지 않았다.

## 현재 동작과 문제

현재 `necrassrs_build::build("schema")`는 SDL에서 계약과 디스패치를 `OUT_DIR/necrassrs.rs`에 생성하고, 편집 가능한 리졸버 구현을 `src/resolvers.rs`에 생성·동기화한다. 새 필드는 `unimplemented!()` 본문을 가진 메서드로 추가하고, 유지된 필드의 메서드 본문은 보존한다. 필드 삭제나 이름 변경은 기존 메서드 삭제와 새 메서드 추가로 처리한다. 따라서 삭제되는 메서드 본문에 작성한 사용자 로직도 함께 사라진다. 사용자가 미리 코드를 복사하지 않았다면 복구가 어려운 워크플로우다.

리졸버가 늘어날수록 모든 메서드가 `src/resolvers.rs`의 한 구현에 모이는 문제도 있다. 현재 예제는 `String!` 기반의 작은 스키마라 이 문제가 드러나지 않지만, 필드 수가 늘어날 때 파일을 나눌 방법이 필요하다.

## 설계 논의

XiNiHa님은 코드 생성된 파일을 사용자가 수정하지 않아도 되는 구조를 제안했다. SDL에서 스키마 계약, 필드 타입, 실행 연결을 생성하고, 사용자는 별도 소스에서 필드별 리졸버를 구현하는 방향이다. 공유받은 `graphql.rs`는 다음 요소를 설명하는 설계 스케치다.

- 필드마다 `Field` 구현으로 인자 타입과 결과 `Shape`를 정의한다.
- 사용자는 `Resolver<F>`를 구현하고 `Output`으로 일반 Rust 값을 반환한다. 예시에서 `Query.user`는 `Option<User>`를 반환하고, `User.name`은 `String`을 반환한다.
- 생성된 객체별 `Complete<T>` 구현은 선택된 하위 필드를 실행하고 결과를 GraphQL 응답 값으로 완성한다. `Nullable<S>` 같은 형태로 출력 nullability를 표현한다.

이 구조에서는 리졸버 구현을 생성 코드와 분리하고 필드별로 여러 모듈에 배치할 수 있다. 다만 사용자가 작성한 `impl Resolver<Query__user>`도 생성된 필드 이름에 의존한다. SDL에서 `user`의 이름을 바꾸면 기존 구현을 자동으로 새 필드에 연결할 근거가 없으며, 사용자가 연결을 수정하거나 명시적인 이름 변경 정보가 필요하다. 사용자 로직을 보존하는 것과 새 이름으로 자동 이전하는 것은 별개다.

XiNiHa님은 사용자 리졸버 코드 자체는 생성하지 않고 직접 작성해도 된다고 제안했다. 자동 생성은 초기 구현의 편의를 위해 유용하므로, 수동 작성 경로를 옵션으로 제공하는 방안도 논의했다. 현재 생성은 CLI 명령이 아니라 소비자 프로젝트의 `build.rs`에서 호출하는 `build()`가 수행하므로, `--auto-resolver` 같은 CLI 플래그만으로는 Cargo 빌드 경로를 제어할 수 없다. 옵션의 이름과 구체적인 API는 아직 결정하지 않았다.

## 검토 메모

새 스케치는 객체 반환과 하위 필드 리졸버의 Rust 타입 연결을 보여주지만, 실행기 대체 구현으로 검증된 코드는 아니다. 현재 실행기가 담당하는 필드 수집·병합, 변수와 인자 coercion, 오류 경로, 부분 결과, null 전파를 새 구조에서 어떻게 유지할지 확인해야 한다. 또한 재귀 객체 및 리스트 반환형의 Rust trait 제약과 컴파일 가능성을 검증해야 한다.

자동 생성 옵션을 끄거나 모드를 전환하더라도 기존 `src/resolvers.rs`를 삭제하거나 덮어써서는 안 된다. 기존 메서드 본문에 있는 로직을 안전하게 분리하는 마이그레이션 방법도 별도 설계가 필요하다.

## 결정 사항

- SDL을 공개 GraphQL 계약으로 사용하고 코드 생성에 의존하는 방향은 유지한다.
- XiNiHa님의 `graphql.rs`는 MVP 이후 객체 결과와 사용자 코드 소유권을 재검토할 때 참고할 설계 후보로 기록한다. 채택이나 구현은 아직 결정하지 않았다.
- 자동 리졸버 생성의 편의성과 수동 구현의 파일 분리 가능성을 모두 고려한다. 수동 모드의 설정 방식, 기본값, 기존 프로젝트 전환 동작은 미정이다.

## 검증

- 변경 수집 스크립트로 2026-09-23의 다음 일지 번호와 저장소 상태를 확인했다.
- `necrassrs`와 `necrassrs-lab` 작업 트리에 변경이 없는 것을 확인했다. 코드 변경이 없어 Rust 빌드 및 테스트는 실행하지 않았다.

## 다음 작업

1. 사용자 리졸버 구현을 생성·동기화 대상에서 분리했을 때 새 필드 추가, 이름 변경, 삭제가 각각 어떻게 동작할지 작은 소비자 예제로 검증한다.
2. 자동 생성과 수동 구현 중 어느 모드에서도 기존 사용자 로직이 조용히 삭제되지 않는 전환 규칙을 정한다.
3. 객체, 재귀 객체, 리스트 및 nullable 결과에서 `Shape` 기반 완료 방식이 컴파일되고 GraphQL 오류·null 전파 규칙을 지키는지 확인한다.
