# 2026-07-22 개발일지 2

## 오늘의 목표

Project relation 구현을 준비하면서, nested GraphQL resolver에서 발생하는 N+1 query를 async-graphql/SQLx 조합이 어떻게 다루게 하는지 검토한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx/src/graphql/project.rs`를 만들기 시작했다.
  - 현재는 OrganizationQuery를 옮기는 중인 작업 상태이며, ProjectRow/Project object/ProjectQuery로 아직 전환되지 않았다.
- `src/graphql/mod.rs`에 project module 선언을 추가했다.
- 현재 `cargo check`는 실패한다.
  - project.rs에 async-graphql, SQLx, Organization 관련 import가 없고, OrganizationQuery 형태가 남아 있어 컴파일할 수 없다.

## 구현 메모

Project query는 이후 다음 relation을 요구한다.

```graphql
project(key: "CORE") {
  organization {
    id
    name
    slug
  }
  issues(first: 10) {
    edges {
      node {
        assignee { id displayName }
        reporter { id displayName }
      }
    }
  }
}
```

Project.organization은 하나의 parent Project에서 하나의 Organization을 조회하므로 단순 nested resolver로 시작할 수 있다. 그러나 Project.issues가 여러 Issue를 반환하면 각 Issue의 assignee와 reporter resolver가 user query를 반복하게 된다. Comment.author까지 선택되면 같은 구조가 한 단계 더 중첩된다.

## 실험 및 관찰

async-graphql은 DataLoader를 제공하지만, relation field에 자동 적용하지 않는다. resolver 작성자가 다음을 모두 결정하고 구현해야 한다.

1. 어떤 relation key를 batch할지 결정한다.
2. key collection을 받아 `WHERE id IN (...)` query를 실행하는 loader를 작성한다.
3. 결과를 key별 map으로 다시 조립한다.
4. DataLoader를 schema data에 등록한다.
5. 각 relation resolver에서 pool 대신 해당 loader를 꺼내 `load_one`을 호출한다.
6. query count와 cache/request scope를 별도 test로 검증한다.

N+1은 resolver를 잘못 작성했을 때만 생기는 우연한 성능 문제가 아니다. GraphQL field가 DB relation과 연결되는 순간 기본적으로 발생 가능한 구조다. 따라서 relation metadata를 가진 framework는 field 선언 시점에 batching 전략을 적용할 수 있어야 한다.

Pothos/Drizzle의 `t.relation`과 `t.relatedConnection`은 relation metadata를 framework/plugin으로 올린다. async-graphql/SQLx에서는 parent struct의 foreign key, nested resolver, DataLoader, batch SQL이 각각 application 코드로 흩어진다. DataLoader가 해결책을 제공하더라도, relation declaration과 N+1 방지 정책 사이의 연결은 사용자의 책임으로 남는다.

이는 relation field가 많은 GraphQL API에서 상당한 구현 및 검증 비용이다. 단순 resolver를 먼저 작성하면 N+1을 놓치기 쉽고, 처음부터 loader를 작성하면 root field 하나를 구현하는 비용이 크게 증가한다.

## 결정 사항

- Project.organization은 우선 직접 SQL query로 구현해 relation resolver의 기본 경계를 확인한다.
- Project.issues와 Issue.assignee/reporter를 구현할 때 DataLoader를 추가해 N+1 완화에 필요한 코드량을 실제로 측정한다.
- Necrass에서는 relation declaration, batch loading policy, connection query plan을 분리하지 않고 함께 표현할 수 있는 구조를 검토한다.

## 다음 작업

- project.rs를 ProjectRow, Project, ProjectQuery 구조로 바로잡고 컴파일을 복구한다.
- Project.organization relation resolver를 구현한다.
- Project.issues 목록 query를 구현한 뒤 assignee/reporter의 query 수를 기록한다.
- User loader의 최소 batch query를 작성해 direct resolver와 비교한다.
