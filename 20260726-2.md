# 2026-07-26 개발일지 2

## 오늘의 목표

`Project.issues`의 forward cursor pagination을 구현하면서 SQLx가 GraphQL connection 구현의 병목이 되는 지점을 확인한다.

## 변경 사항

- `services/api-async-graphql-sqlx/src/graphql/project.rs`
  - `after`가 있을 때 `OpaqueCursor<(updated_at, id)>`를 분해해 keyset pagination 조건을 SQL에 추가했다.
  - `updated_at DESC, id DESC` 정렬에 맞춰 `(updated_at < cursor_updated_at) OR (updated_at = cursor_updated_at AND id < cursor_id)` 조건을 사용했다.
  - `has_previous_page`, `has_next_page`, edge 변환을 connection에 연결했다.
- `cargo check`가 성공했다. `filter`, `order_by` 미사용과 기존 row/object 필드 미사용에 대한 경고는 남아 있다.

## 구현 메모

`async_graphql::connection::query`는 HTTP/GraphQL 입력 문자열인 `after`를 resolver closure에 `Option<OpaqueCursor<(String, String)>>`으로 전달한다. cursor가 없을 때와 있을 때의 SQL은 cursor predicate 유무가 다르므로 현재 구현은 `match after`로 두 쿼리를 작성했다.

두 분기 모두 다음 규칙을 유지한다.

- `first + 1`개를 조회한다.
- 추가 row가 있으면 `has_next_page`를 true로 설정한다.
- 응답 전에는 `first`개로 자른다.
- 반환 row마다 `(updated_at, id)` cursor와 `Issue` node를 만들어 edge로 변환한다.

## 실험 및 관찰

GraphQL connection의 cursor, page size, page info는 async-graphql이 일부 지원하지만 relation query 자체는 SQLx의 raw SQL로 작성해야 한다.

현재 `after` 하나만 추가해도 SELECT 목록, FROM, 기본 WHERE, ORDER BY, LIMIT, `fetch_all`, 오류 변환이 두 branch에 중복되었다. 다음 요구 사항인 `filter.status`, `filter.assignee_id`, `filter.search`, `order_by.direction`를 같은 방식으로 추가하면 가능한 조건 조합마다 SQL 본문이 증가한다.

SQLx는 타입이 지정된 row mapping과 bind parameter를 제공하지만, Pothos/Drizzle의 `relatedConnection`처럼 relation metadata와 GraphQL 인자를 query로 조립하지 않는다. resolver가 SQL 식별자, 조건문, 정렬 방향, cursor 비교 연산자, page info 계산까지 직접 소유한다. 이 경계가 현재 실험에서 가장 큰 구현 비용이다.

## 결정 사항

- 다음 단계에서 `QueryBuilder`를 사용해 공통 SELECT와 `WHERE project_id = ?`를 한 번만 유지한다.
- `filter`와 `order_by`를 적용할 때는 SQL 조립과 cursor predicate를 하나의 resolver 안에서 함께 처리한다.
- 별도 repository나 query abstraction은 아직 추가하지 않는다. 현재 문제는 SQL 조건 조립이며, 한 개의 connection resolver를 일반화하는 것은 이르다.

## 다음 작업

- `QueryBuilder`로 `status`, `assignee_id`, `title LIKE` filter를 추가한다.
- `OrderDirection::Asc`와 `OrderDirection::Desc`에 따라 ORDER BY 및 cursor 비교 연산자를 함께 전환한다.
- `after`를 사용한 두 페이지 조회와 filter 조합을 GraphQL 테스트로 검증한다.
