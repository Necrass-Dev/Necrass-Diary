# 2026-07-10 개발일지 2

## 오늘의 목표

async-graphql 문서의 `InputObject` generic 예시를 읽으면서, async-graphql이 Rust 코드를 작성한다기보다 Rust 문법 안에서 매크로 DSL을 작성하게 만든다는 첫인상을 더 구체화했다. 이전 일지에서 남긴 "러스트가 아니라 매크로로 작성하는 느낌"을 generic input object 사례로 검증한다.

## 변경 사항

- 저장소 코드 변경은 없었다.
- `necrassrs-lab/services/api-async-graphql-sqlx`는 여전히 async-graphql 실험을 시작하기 위한 초기 상태다.
  - `Cargo.toml`에는 `async-graphql = { version = "7.2.1", features = ["dataloader"] }`와 `async-graphql-axum = "7.2.1"`이 들어 있다.
  - `sqlx`는 `runtime-tokio`, `sqlite`, `macros`, `migrate` feature를 켠 상태다.
  - `src/main.rs`는 아직 `Hello, world!`만 출력한다.
- 이번 기록은 코드 구현보다 문서 API 표면에서 보이는 설계 감각을 정리하는 데 초점을 둔다.

## 구현 메모

async-graphql 문서의 generic input object 예시는 다음 형태다.

```rust
#[derive(InputObject)]
#[graphql(concrete(name = "SomeName", params(SomeType)))]
#[graphql(concrete(name = "SomeOtherName", params(SomeOtherType)))]
pub struct SomeGenericInput<T: InputType> {
    field1: Option<T>,
    field2: String,
}
```

이 예시는 GraphQL에는 generic type이 없고 Rust에는 generic type이 있다는 충돌을 그대로 드러낸다. Rust 관점에서는 `SomeGenericInput<T>` 하나의 타입을 정의한 것처럼 보이지만, GraphQL schema 관점에서는 `SomeName`, `SomeOtherName`이라는 별도 concrete input object를 만들어야 한다. 그래서 Rust의 generic parameter `T`가 GraphQL 타입 시스템으로 자연스럽게 투영되는 것이 아니라, 매크로 attribute에 concrete name과 params를 반복해서 적는 방식으로 해결된다.

문제는 이 표현이 boilerplate를 줄인다기보다, 계약의 중요한 부분을 attribute 안의 작은 DSL로 밀어 넣는다는 점이다. `SomeGenericInput<T>`의 Rust 정의만 보면 GraphQL schema에 어떤 input object 이름들이 생기는지 알 수 없다. 실제 schema 이름, generic instantiation 목록, GraphQL input type 등록 방식은 `#[graphql(concrete(...))]` attribute를 해석해야만 보인다.

이 방식은 특히 Necrass가 피하고 싶은 구조다. GraphQL 계약의 핵심 정보가 Rust 타입 정의의 본문이 아니라 proc-macro attribute에 들어가고, 그 attribute가 다시 GraphQL type name과 Rust generic argument를 이어 붙인다. 타입 안전성을 얻는 대신 source of truth가 Rust type, GraphQL schema, macro attribute 사이에 갈라지는 느낌이 강하다.

## 실험 및 관찰

이번 예시에서 가장 크게 느껴진 점은 "Rust의 타입 시스템을 활용한다"와 "Rust 문법으로 GraphQL 매크로 설정을 작성한다"가 다르다는 것이다. `T: InputType` bound는 Rust답지만, 실제 GraphQL 계약을 확정하는 부분은 `#[graphql(concrete(...))]`이다. 즉, 사용자는 Rust generic을 설계하는 동시에, GraphQL에는 generic이 없다는 사실을 attribute 반복으로 수동 보정해야 한다.

이것은 작은 예제에서는 감수할 수 있다. 하지만 Necrass가 다루려는 서버 프레임워크 수준에서는 input object, payload, connection, filter, order, nested mutation input이 계속 늘어난다. 그때마다 generic helper를 재사용하려면 concrete mapping을 attribute로 열거해야 하고, schema 이름도 문자열처럼 관리해야 한다. 이 지점부터는 Rust 컴파일러가 직접 이해하는 타입 설계라기보다 async-graphql 매크로가 요구하는 설정 파일을 Rust 위에 얹는 감각이 강해진다.

Pothos도 builder DSL을 사용하지만, 그 DSL은 명시적으로 builder 호출의 세계에 있다. 반면 async-graphql의 derive/attribute API는 일반 Rust struct처럼 보이는 코드에 GraphQL 계약 생성 규칙을 숨긴다. 이 차이는 리뷰와 디버깅에서 중요하다. Pothos 코드는 "여기는 schema builder다"라는 신호가 분명하지만, async-graphql 코드는 "일반 Rust 타입처럼 보이는 매크로 입력"이 된다.

`Option<T>`가 `field1`에 들어간 것도 별도 관찰 지점이다. GraphQL input에서 `null`, 생략, 값 전달은 같은 문제가 아니다. generic input object 예시가 `Option<T>`를 자연스럽게 보여주지만, Necrass 입장에서는 `Option<T>` 하나로 absent/null/value를 모두 설명할 수 없다는 점을 계속 경계해야 한다. async-graphql이 실제로는 `MaybeUndefined<T>` 같은 별도 타입을 제공한다는 사실은, 이 문제가 예제 수준의 문법보다 더 깊다는 신호다.

## 결정 사항

- async-graphql의 generic `InputObject` 지원은 기능적으로 유용할 수 있지만, Necrass의 설계 기준으로는 계약 정보가 매크로 attribute에 과하게 들어가는 사례로 본다.
- Necrass에서는 GraphQL에 generic이 없다는 제약을 사용자가 attribute 반복으로 보정하게 만들기보다, 계약 IR 또는 SDL/codegen 단계에서 concrete type 생성을 명시적으로 다루는 방향을 우선 검토한다.
- GraphQL schema 이름은 Rust generic instantiation의 부수 효과로 생기게 하지 않는다. schema name과 Rust generated type name의 대응 관계를 codegen 산출물에서 추적 가능하게 만든다.
- input object의 optional field는 `Option<T>` 하나로 모델링하지 않고, absent/null/value 의미를 코어 타입과 coercion 단계에서 분리한다는 원칙을 유지한다.

## 다음 작업

- `api-async-graphql-sqlx`에서 가장 작은 `InputObject`와 mutation을 직접 작성해, 문서 예시에서 느낀 매크로 DSL 감각이 실제 구현에서도 반복되는지 확인한다.
- generic input helper가 필요한 사례를 일부러 만들어보고, `#[graphql(concrete(...))]`가 schema 출력과 compile error에 어떻게 드러나는지 기록한다.
- schema export 또는 snapshot test를 붙여, attribute 변경이 GraphQL 계약 변경으로 어떻게 보이는지 확인한다.
- 같은 패턴을 Necrass의 contract-first codegen으로 표현한다면 어떤 generated Rust 코드가 나와야 하는지 초안을 잡는다.
