# 2026-07-26 NecrassRs 개발 기록

## 개요

NecrassRs는 Rust의 정적 타입 체계를 GraphQL에 단순히 덧씌우는 프레임워크가 아니라, GraphQL 계약과 Rust 구현 사이의 경계를 다시 설계하려는 프로젝트다. 초기 조사와 Pothos/Drizzle, async-graphql/SQLx 비교 실험을 통해 다음 문제가 반복해서 확인됐다.

- GraphQL의 nullable 기본 규칙과 Rust의 `Option<T>` 중심 표현은 직접 대응하지 않는다.
- 입력값의 생략, 명시적 `null`, 실제 값은 서로 다른 상태다.
- code-first 매크로는 빠른 시작을 돕지만 계약의 위치를 Rust 타입, attribute, 생성된 schema로 분산시킨다.
- DB relation과 GraphQL field 사이의 연결이 없으면 resolver, batching, pagination, mapping을 애플리케이션이 반복 구현해야 한다.
- 규칙적인 코드는 생성할 수 있지만 인증, 도메인 검증, 트랜잭션, 복잡한 relation query까지 일반화하면 제어 흐름이 오히려 불투명해진다.

현재까지의 결론은 NecrassRs가 구조화된 schema IR을 중심에 두고, 정형적인 GraphQL 및 DB 연결 코드를 생성하되 비정형적인 비즈니스 로직은 명시적인 hook으로 남겨야 한다는 것이다.

## 배경

### Rust와 GraphQL의 모델 차이

GraphQL은 타입을 기본적으로 nullable로 보고 `List`와 `Non-Null` wrapper를 조합한다. Rust는 값의 부재를 `Option<T>`로 나타내므로 기본 방향이 반대다. 이 차이를 SDL 문자열로만 관리하면 wrapper 조합, subtype 판정, list 원소의 nullability 같은 구조적 검증이 취약해진다.

입력값에서는 차이가 더 크다. GraphQL mutation의 optional field는 다음 세 상태를 구분해야 한다.

1. 필드 생략: 기존 값을 변경하지 않는다.
2. 명시적 `null`: 기존 값을 제거한다.
3. 값 전달: 새 값으로 변경한다.

`Option<T>`만으로는 이 세 상태를 보존할 수 없다. async-graphql의 `MaybeUndefined<T>`와 Juniper의 `Nullable<T>`가 별도로 존재하는 것도 이 의미론이 GraphQL 실행 모델의 핵심임을 보여 준다.

Interface와 union도 Rust trait에 그대로 대응하지 않는다. GraphQL interface는 추상 타입인 동시에 실행 결과의 값이지만, Rust trait는 그 자체로 값 타입이 아니다. 따라서 구현 타입의 열거, enum wrapper, trait object, runtime downcast 같은 별도 표현이 필요하다.

### 기존 Rust 구현체 조사

async-graphql은 넓은 기능 범위와 동적 schema API를 제공하지만, 타입 표현이 계층마다 일관되지는 않는다. 동적 API의 `TypeRef`는 `Named`, `List`, `NonNull`로 구조화되어 있으나 registry와 일부 macro surface에서는 타입이 문자열로 다시 표현된다. code-first와 proc-macro 중심 API는 작은 schema에서는 편리하지만, 계약의 실제 형태를 Rust 코드와 macro 확장 사이에 분산시킨다.

Juniper는 안정적인 code-first 구현체지만 GraphQL SDL을 기본 source of truth로 삼지 않는다. Rust 타입의 자연스러움을 우선해 non-null을 기본으로 투영하고, interface 구현 타입을 열거해야 하며, 동적 schema와 최신 spec 지원은 별도 비용을 요구한다.

두 구현체 모두 N+1 문제를 DataLoader나 eager loading 패턴으로 완화할 수 있지만, relation 선언과 batching 정책의 연결은 주로 사용자 책임이다. 이 조사에서 NecrassRs의 초기 방향으로 다음 원칙을 도출했다.

- SDL 문자열은 입출력 경계에서만 사용하고 내부 타입은 wrapper tree로 표현한다.
- `absent / null / value`를 coercion부터 resolver 인자까지 보존한다.
- GraphQL 계약을 canonical source로 두고 Rust 코드를 생성한다.
- interface와 union의 등록 및 dispatch 코드는 생성 단계에서 완성한다.
- batching을 부가 helper가 아니라 relation 실행 정책으로 다룬다.
- 목표 GraphQL spec 판본과 conformance test를 명시한다.

## 구현 과정

### Pothos와 Drizzle로 기준 API 구성

먼저 TypeScript 환경에서 Pothos, GraphQL Yoga, Drizzle을 조합해 비교 기준이 될 API를 구현했다. 개발 실행에는 `tsx`, 빌드에는 `tsdown`을 사용했다. Pothos 자체의 schema builder는 비교적 단순했지만 migration, seed, 개발 실행, ESM, build output까지 포함한 TypeScript 서버 pipeline은 별도의 도구 구성을 요구했다.

Pothos 실험에서는 다음 API를 구현했다.

- `viewer`
- `organization(slug:)`
- `project(key:)`
- `project.issues`
- `createIssue`
- `updateIssue`
- `changeIssueStatus`
- `addComment`
- `changeMemberRole`

Mutation payload는 entity와 `UserError` 목록을 함께 반환하도록 통일했다. `updateIssue`에서는 JavaScript property 존재 여부를 검사해 생략, `null`, 값 전달을 구분했다.

Pothos Drizzle plugin은 table metadata와 GraphQL type을 연결했다. 단순 column은 `expose` 계열 API로, 단일 relation은 `relation`, collection은 `relatedConnection`으로 선언할 수 있었다. 이 방식은 schema builder라는 별도 DSL에 의존하지만, root field, object field, relation field의 역할이 호출 형태에 명시적으로 드러났다.

### async-graphql과 SQLx로 동일 API 이식

Rust 비교 실험은 Axum, async-graphql, SQLx, SQLite로 구성했다. Pothos 실험과 별도의 SQLite database와 migration을 사용하고, reset 명령에서 migration과 seed를 적용하도록 분리했다. GraphQL schema는 HTTP를 통하지 않고 `Schema::execute`로 직접 검증할 수 있게 구성했다.

HTTP header 기반의 개발용 인증은 Axum handler에서 `x-user-id`를 추출해 `RequestContext`로 변환한 뒤 async-graphql request data에 주입했다. resolver가 HTTP request 전체에 의존하지 않는다는 장점이 있었지만, `viewer` 하나에도 handler extractor, request context, data 주입, resolver context 조회가 필요했다.

DB row와 GraphQL output object는 의도적으로 분리했다. 내부 column인 password와 login 상태가 GraphQL에 노출되지 않는 안전한 경계가 생겼지만, 각 object마다 다음 정보가 반복됐다.

- SQLx `FromRow` 구조체
- GraphQL output 구조체
- row-to-object 변환
- `#[Object]` getter method
- root query container와 `MergedObject` 등록

`User`, `Organization`, `Issue`, `Project`, `Comment`로 타입이 늘어나면서 반복은 누적됐다. root field container와 일반 output object가 동일한 `#[Object]` macro를 사용하므로, schema의 역할을 파악하려면 type 이름과 `MergedObject` 조립 코드까지 함께 읽어야 했다.

### Relation과 N+1 경계

Pothos/Drizzle의 relation metadata에 대응하는 계층은 async-graphql/SQLx 조합에 없었다. `Issue.assignee`, `Issue.reporter`, `Comment.author` 같은 field를 구현하려면 parent에 foreign key를 보관하고, resolver에서 pool 또는 DataLoader를 꺼내 직접 SQL을 실행해야 한다.

여러 Issue를 반환하는 `Project.issues`에서는 각 relation resolver가 개별 query를 실행할 수 있으므로 N+1 문제가 구조적으로 발생한다. async-graphql의 DataLoader를 사용하려면 batch key, `IN` query, 결과 map, schema 등록, request scope cache, query count 검증을 모두 애플리케이션이 책임져야 한다.

이 결과로 relation 선언, batch loading policy, connection query plan을 서로 분리된 부가기능으로 두지 않는 방향이 NecrassRs의 핵심 후보가 됐다.

### Relay connection 구현

`Project.issues(after, first, filter, orderBy)`에는 status, order field, direction enum과 filter/order input을 정의했다. 기본 정렬은 `updated_at DESC, id DESC`로 두고 두 값을 함께 opaque cursor로 사용했다. 동일한 갱신 시각을 가진 row 사이에서도 순서를 안정적으로 유지하기 위한 결정이다.

Forward pagination은 `first + 1`개를 조회해 `hasNextPage`를 계산하고, 응답에는 `first`개만 포함했다. `after`가 있으면 다음 keyset 조건을 적용했다.

```sql
updated_at < ?
OR (updated_at = ? AND id < ?)
```

async-graphql은 cursor decoding과 connection 응답 타입을 제공했지만 SQL predicate, filter, order direction, row-to-edge 변환, page info 계산은 resolver가 담당했다. cursor 유무만으로 SQL 본문이 중복됐고, filter와 정렬 방향까지 조합하면 branch가 빠르게 증가할 것으로 확인됐다. 이에 SQLx `QueryBuilder`로 조건을 한 resolver 안에서 조립하되, connection 하나를 위한 repository abstraction은 만들지 않기로 했다.

### Node interface와 global ID

Relay global ID는 type discriminator와 raw database ID를 encode하도록 구성했다. `node(id:)`는 ID를 decode한 뒤 `User`, `Project`, `Issue`, `Comment` 중 해당 table을 조회해 `Node` interface variant로 반환한다.

Interface derive가 각 object의 `id` method를 sibling module에서 호출하기 때문에 method visibility를 `pub(crate)`로 넓혀야 했다. Node lookup의 제어 흐름은 connection보다 단순했지만 새 타입 하나를 추가할 때 다음 변경이 반복됐다.

- row type과 GraphQL object 추가
- global ID discriminator 확장
- Node interface variant 추가
- type별 SELECT와 변환 branch 추가
- module과 root 등록

세 종류에서 네 종류로 확장해도 제어 흐름은 선형이었기 때문에 generic repository나 factory는 추가하지 않았다. 대신 이 반복을 schema IR과 code generation의 구체적인 대상으로 분류했다.

### Mutation 구현

async-graphql MVP에서는 `createIssue`, `updateIssue`, `addComment`, `changeIssueStatus`를 구현했다. 각 mutation은 input과 payload, 인증, global ID 검증, 대상 및 relation entity 조회, domain validation, SQL 실행, row mapping, root 등록을 반복했다.

`updateIssue`는 `MaybeUndefined`로 생략, `null`, 값을 구분하고 변경할 column과 bind 값을 직접 조립했다. `addComment`는 comment 삽입과 issue 갱신 시각 변경을 하나의 transaction으로 묶었다. 이 과정에서 Query보다 Mutation이 입력 의미론, 오류, validation, transaction이라는 추가 축을 가진다는 점이 확인됐다.

범용 mutation repository는 만들지 않았다. 인증과 validation, transaction 경계는 mutation마다 다르기 때문에 런타임 추상화로 감추면 코드량보다 제어 흐름의 거리가 더 늘어날 가능성이 컸다.

## 실험에서 확인한 점

### 확인된 결과

1. GraphQL object type과 root query field는 별개의 schema 요소다. Pothos의 object 등록만으로 query가 생기지 않았고, async-graphql에서도 output object와 root container를 따로 구현해야 했다.
2. DB row와 GraphQL object의 분리는 내부 column 노출을 막지만, 연결 metadata가 없으면 mapping boilerplate를 만든다.
3. `Option<T>`는 partial update의 입력 의미론을 보존하지 못한다. `MaybeUndefined<T>` 같은 삼중 상태 표현이 필요하다.
4. async-graphql/SQLx에서 connection 구현의 병목은 edge 변환이 아니라 동적 SQL 조건, cursor predicate, relation, page info의 결합이다.
5. DataLoader의 존재만으로 N+1이 자동으로 해결되지 않는다. relation resolver와 batch policy를 직접 연결해야 한다.
6. Node interface의 dispatch는 단순하지만 type 추가 정보가 여러 파일에 분산된다.
7. Mutation의 규칙적 반복과 비정형 domain logic은 같은 방식으로 일반화할 수 없다.
8. async-graphql 실험 코드는 formatter, compiler, diff check를 통과했으며 1차 mutation 범위까지 구현됐다.

### 아직 확인되지 않은 부분

- `Project.issues`의 filter와 정렬 방향 조합은 아직 완성되지 않았다.
- 여러 페이지와 filter 조합을 검증하는 GraphQL test가 필요하다.
- `Issue.comments` connection과 `Comment.author`를 통한 두 번째 N+1 사례는 구현되지 않았다.
- Pothos와 async-graphql 결과의 SDL 및 동작 비교가 남아 있다.
- schema IR과 code generator는 방향만 정했으며 최소 prototype은 아직 없다.
- 실행기 수준의 batching 정책과 GraphQL spec conformance 범위는 설계 가설 단계다.

## 설계 결정

### 계약과 내부 표현

GraphQL 계약을 canonical source로 두고, 내부 타입은 `Named`, `List`, `NonNull`을 표현하는 구조화된 tree로 유지한다. SDL 문자열은 parsing과 출력 경계에서만 사용한다.

입력값은 `absent / null / value`를 독립된 상태로 보존한다. 이 상태는 parser나 validator에서 사라지지 않고 generated resolver signature와 mutation update logic까지 이어져야 한다.

### 생성할 코드의 범위

schema IR 또는 이에 준하는 단일 선언에서 다음 정형 코드를 생성하는 방향을 우선한다.

- GraphQL object와 input/payload type
- 단순 DB column field와 row mapping
- global ID encode/decode와 Node dispatch
- interface/union 등록
- root operation 등록
- nullable 및 scalar coercion 경계
- resolver trait 또는 function signature

반면 다음 요소는 사용자가 작성하는 명시적 hook으로 남긴다.

- 인증과 권한 검사
- 도메인 validation
- transaction 경계
- computed field
- 복잡한 relation, filter, order, connection query

Relation metadata가 충분한 경우 batch loading과 connection query plan을 생성하거나 정책으로 연결할 수 있어야 하지만, 이를 모든 resolver를 감추는 범용 repository로 만들지는 않는다.

### API와 개발 경험

GraphQL root type은 `Query`, `Mutation`, `Subscription` 기본 이름을 사용해 불필요한 schema mapping과 구현체 간 SDL 차이를 피한다. 생성된 Rust 코드는 rust-analyzer와 compiler가 직접 이해할 수 있어야 하며, 중요한 계약 정보를 proc-macro attribute 문자열에 숨기지 않는다.

현재 실험 코드에는 repository, factory, trait hierarchy 같은 범용 abstraction을 추가하지 않는다. 반복의 형태를 충분히 확인한 뒤 생성 가능한 부분만 제거한다.

## 남은 과제

1. Pothos와 async-graphql MVP의 SDL 및 API 동작을 동일한 요청으로 비교한다.
2. `Project.issues`의 filter, 정렬 방향, keyset pagination을 완성하고 연속 페이지 test를 작성한다.
3. `Issue.comments`와 `Comment.author`를 구현해 relation pagination과 N+1 비용을 측정한다.
4. Query와 Mutation에서 반복된 선언을 schema IR 항목으로 정리한다.
5. User 또는 Comment 한 타입을 대상으로 row mapping, global ID, Node dispatch를 생성하는 최소 prototype을 만든다.
6. custom resolver hook이 인증, validation, transaction을 명시적으로 유지하는지 검증한다.
7. 지원할 GraphQL spec 판본과 conformance test 범위를 정한다.
8. relation declaration과 batching, connection query plan을 연결하는 최소 실행 모델을 설계한다.
