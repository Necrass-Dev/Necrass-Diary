# 2026-07-10 개발일지 5

## 오늘의 목표

Pothos의 `viewer` root field 구현과 async-graphql/Axum에서 request header를 resolver context로 전달하는 방식을 비교하고, integration layer가 만드는 구현 비용을 확인한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx/src/graphql/context.rs`에 `RequestContext`를 추가했다.
  - 현재는 `x-user-id`에서 얻은 `Option<String>`만 담는다.
- `src/graphql/user.rs`에 `User` GraphQL object의 Rust struct와 `ViewerQuery`를 추가했다.
- `src/graphql/mod.rs`는 `ViewerQuery`를 `MergedObject`로 합쳐 GraphQL root `Query`를 만들기 시작했다.
- `src/main.rs`의 GraphQL handler는 Axum `HeaderMap`에서 `x-user-id`를 읽어 `RequestContext`를 만든 뒤, `Request::data(context)`로 async-graphql 실행 요청에 주입하도록 변경됐다.

## 구현 메모

Pothos의 viewer 구현은 GraphQL Yoga request를 context에 그대로 넣고, resolver가 `ctx.request.headers.get("x-user-id")`로 header를 읽는다. `builder.queryField("viewer")`는 `UserRef`와 Drizzle query를 직접 연결한다.

async-graphql의 resolver `Context`는 Axum request나 HTTP header를 기본으로 노출하지 않는다. Axum handler에서 header를 추출하고, async-graphql `Request` data로 별도의 context 값을 주입해야 resolver가 이를 읽을 수 있다.

```rust
let context = RequestContext {
    user_id: headers
        .get("x-user-id")
        .and_then(|value| value.to_str().ok())
        .map(str::to_owned),
};

schema.execute(req.into_inner().data(context)).await;
```

`ViewerQuery`라는 이름은 현재 단일 root field인 `viewer`의 의도를 드러낸다. 이후 user 도메인의 root field가 늘어나면 `UserQuery`로 넓힐 수 있다. `MergedObject`는 Pothos처럼 도메인 파일에 GraphQL object와 root resolver를 함께 두기 위한 async-graphql의 표준 분할 수단이다.

## 실험 및 관찰

integration layer는 HTTP framework와 GraphQL resolver를 분리한다는 장점이 있다. `Schema::execute` 테스트에서는 HTTP request 없이 `RequestContext::default()` 또는 원하는 actor context를 넣어 resolver를 검증할 수 있다.

반면 단순한 `viewer` 하나를 만들기 위해 Axum `HeaderMap`, custom request context, `Request::data`, resolver의 `ctx.data`를 모두 연결해야 한다. Pothos/GraphQL Yoga에서는 이미 제공되는 request context를 읽으면 되는 흐름과 비교해, framework integration의 세부 계약이 application resolver에 도달하기 전 별도 코드로 드러난다.

현재 `cargo check`는 실패한다. `RequestContext`가 `graphql` module 최상위에 re-export되지 않았고, `main.rs`에 `HeaderMap` import가 없다. 또한 `ViewerQuery`에는 `Context`, async-graphql `Result` import와 실제 resolver body가 없으며, `User`에는 아직 `#[Object]` 구현이 없다. 따라서 request context 전달 방식은 작성 중이며 아직 실행 검증되지 않았다.

## 결정 사항

- resolver는 Axum `HeaderMap`을 직접 받지 않고, request별 필요한 정보를 담은 `RequestContext`만 받는다.
- `RequestContext`에는 인증된 actor처럼 GraphQL 실행에 필요한 값만 넣고, HTTP request 전체를 무분별하게 전달하지 않는다.
- Pothos 대비 추가되는 integration layer의 코드량과 테스트 편의성은 이후 viewer 구현 완료 후 다시 평가한다.

## 다음 작업

- `RequestContext`를 `graphql` module에서 re-export하고, handler의 `HeaderMap` extractor/import를 완성한다.
- `User` GraphQL object와 DB `UserRow` 변환을 구현한다.
- `ViewerQuery.viewer`에서 context의 user_id를 읽어 users 테이블을 조회한다.
- Schema 직접 실행 test와 Axum HTTP test에서 각각 header/context 전달을 검증한다.
