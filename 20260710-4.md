# 2026-07-10 개발일지 4

## 오늘의 목표

async-graphql의 root operation type 이름과 GraphQL 명세의 기본 root type 이름 규칙을 대조하고, Pothos와 동일한 schema contract를 유지하기 위한 명명 원칙을 결정한다.

## 변경 사항

- `necrassrs-lab/services/api-async-graphql-sqlx/src/main.rs`에는 Rust 구현체 이름으로 `QueryRoot`가 남아 있다.
- `src/graphql/mod.rs`를 추가하는 과정에서 schema 구성 코드를 분리하려 했지만, 최상위 `let` 문은 Rust module item이 아니므로 아직 컴파일되지 않는다.
- 이번 작업에서는 root object의 GraphQL 이름을 기본 이름인 `Query`로 맞추기로 결정했다.

## 구현 메모

GraphQL 명세는 query, mutation, subscription의 기본 root operation type 이름을 각각 `Query`, `Mutation`, `Subscription`으로 정의한다. 이 기본 이름을 사용하면 SDL에서 별도 schema definition 없이 root operation type을 알 수 있다.

다른 이름도 유효하다. 예를 들어 `QueryRoot`를 GraphQL object 이름으로 사용하면 schema는 개념적으로 `schema { query: QueryRoot }`라는 명시적 root operation mapping을 가진다. 이는 명세 위반이 아니지만, 기본 이름을 사용하는 경우에는 필요 없는 schema 표현이다.

async-graphql의 Rust struct 이름이 곧 GraphQL object 이름의 기본값이 된다. 따라서 Rust 구현체를 `QueryRoot`로 선언하면 GraphQL schema의 root object도 `QueryRoot`가 된다. `#[Object(name = "Query")]` attribute로 이름을 덮어쓸 수 있지만, Rust 이름과 GraphQL schema 이름이 다시 분리된다.

## 실험 및 관찰

Pothos 구현은 `builder.queryType({})`, `builder.mutationType({})`를 사용하며 GraphQL schema의 기본 root name인 `Query`, `Mutation`을 사용한다. async-graphql 구현도 같은 API contract를 비교하려면 introspection 및 SDL에서 동일한 root type 이름을 유지하는 편이 낫다.

`QueryRoot`라는 구현체 이름은 async-graphql 예제에서 흔히 보이지만, Rust 코드의 구분 편의 외에 GraphQL schema 이름을 달리해야 할 필요를 제공하지는 않는다. 특히 별도 `schema { query: QueryRoot }` mapping이 필요한 schema는 더 많은 표현을 요구하고, Pothos 결과와의 diff에도 불필요한 차이를 만든다.

## 결정 사항

- async-graphql 실험의 query root Rust struct와 GraphQL object 이름을 모두 `Query`로 둔다.
- mutation과 subscription을 구현할 때도 각각 `Mutation`, `Subscription`을 기본 이름으로 사용한다.
- Rust 구현체 이름을 `QueryRoot`로 유지하면서 GraphQL name attribute로 `Query`를 덮어쓰는 방식은 사용하지 않는다.
- root type 기본 이름을 따름으로써 별도 root operation mapping 없이 SDL과 introspection 결과를 Pothos와 맞춘다.

## 다음 작업

- `src/main.rs`의 `QueryRoot`를 `Query`로 바꾸고 `AppSchema` alias와 `Schema::build` 호출을 함께 갱신한다.
- `src/graphql/mod.rs`의 실행문을 제거하고, schema 구성 함수 또는 test module로 정리한다.
- schema SDL 또는 introspection test로 root type 이름이 `Query`인지 확인한다.
